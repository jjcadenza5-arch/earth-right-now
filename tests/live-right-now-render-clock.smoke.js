import fs from "node:fs";const a=fs.readFileSync("src/app.js","utf8");console.assert(a.includes("const now=new Date(),model=liveRightNowModel(all,{places,limit:8,taste:currentTaste(),now})"),"Live Right Now model must use captured render moment");console.assert(a.includes("liveWindowView(s,{index,now,onOpen:x=>sourceAction(x,{surface:\"live-right-now\"})})"),"Live Right Now cards must share model render moment");console.log("ERN Live Right Now render-clock checks passed");