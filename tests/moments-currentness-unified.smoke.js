import { happeningNowEligible,rankMoments } from "../src/moments-engine.js";const now=new Date().toISOString(),base={permission:"LINK_ONLY",health:"HEALTHY",playback:"EXTERNAL",truth:"EXTERNAL_LIVE",sourceUrl:"https://example.com",checkedAt:now,lastSuccessfulCheck:now,quality:60,freshness:60,moment:80,categories:["Earth Happening Now"]};console.assert(happeningNowEligible(base));const stale={...base,checkedAt:"2020-01-01",lastSuccessfulCheck:"2020-01-01"};console.assert(!happeningNowEligible(stale));console.assert(rankMoments([stale,base])[0]===base);console.log("ERN Moments currentness smoke checks passed");