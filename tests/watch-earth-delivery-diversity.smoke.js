import { diversifyDeliveryHosts } from "../src/watch-earth-delivery-diversity.js";const rows=[1,2,3,4,5].map(i=>({id:"a"+i,sourceUrl:"https://a.example/"+i})).concat([{id:"b",sourceUrl:"https://b.example/live"}]);const x=diversifyDeliveryHosts(rows,{limit:5,maxPerHost:4});console.assert(x.some(s=>s.id==="b"),"another delivery host should enter before a fifth same-host source");const y=diversifyDeliveryHosts(rows.slice(0,5),{limit:5,maxPerHost:4});console.assert(y.length===5,"soft cap must relax rather than leave Watch Earth underfilled");console.log("ERN Watch Earth delivery diversity checks passed");