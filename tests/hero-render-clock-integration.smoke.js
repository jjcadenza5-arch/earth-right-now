import fs from "node:fs";const a=fs.readFileSync("src/app.js","utf8");for(const x of ["function setHero(s,{now=new Date()}={})","favoritesCount:favorites.size,now","const now=new Date(),next=nextHeroSource(heroSources,current?.id,now)","setHero(next,{now})","setHero(current,{now:bootMoment})",'rankMoments(all,{now:bootMoment}).slice(0,12)'])console.assert(a.includes(x),"hero/initial moments must share render clocks: "+x);console.log("ERN hero render-clock integration checks passed");