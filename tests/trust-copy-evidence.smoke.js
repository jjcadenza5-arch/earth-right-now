import { trustLine } from "../src/trust-copy.js";const now=new Date().toISOString(),base={health:"HEALTHY",permission:"LINK_ONLY",checkedAt:now,lastSuccessfulCheck:now,sourceUrl:"https://example.com"},image={...base,truth:"LIVE_IMAGE",playback:"EXTERNAL"},fresh={...image,freshnessEvidence:"provider refresh evidence"},video={...base,truth:"LIVE_VIDEO",playback:"EXTERNAL"};console.assert(trustLine(image).includes("freshness is not confirmed"),"unverified image trust copy must not imply current visuals");console.assert(trustLine(fresh).includes("near-now view"),"evidenced refreshed image may use near-now trust copy");console.assert(trustLine(video).includes("near-now view"),"verified live video may use near-now trust copy");console.log("ERN trust-copy evidence checks passed");