import { liveInventory } from "../src/live-inventory.js";const now=new Date().toISOString(),rows=[{id:"a",truth:"LIVE_VIDEO",health:"HEALTHY",permission:"LINK_ONLY",playback:"EXTERNAL",sourceUrl:"https://example.test/a",checkedAt:now,lastSuccessfulCheck:now},{id:"b",truth:"LIVE_VIDEO",health:"UNKNOWN",permission:"LINK_ONLY",playback:"EXTERNAL",sourceUrl:"https://example.test/b",checkedAt:now}];console.assert(liveInventory(rows).map(x=>x.id).join(",")==="a");console.log("ERN live inventory smoke checks passed");