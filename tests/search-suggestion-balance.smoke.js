import { earthSuggestions } from "../src/earth-suggestions.js";const x=earthSuggestions({currentAvailable:true,limit:7,includeRecent:false});console.assert(x.some(s=>s.query==="Chiang Mai"),"Search Earth suggestions should include a direct destination example");console.assert(x.every(s=>s.query.trim()),"suggestion chips must always execute a real search");console.assert(x.some(s=>s.current)&&x.some(s=>!s.current),"suggestions should balance right-now discovery with destination search");console.log("ERN Search Earth suggestion balance checks passed");