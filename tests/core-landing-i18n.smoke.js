import fs from "node:fs";import {t,supportedLanguages} from "../src/language.js";const h=fs.readFileSync("index.html","utf8");for(const key of ["heroWelcome","heroPromise","heroDeck","chooseWindowTitle","chooseWindowCopy","watchEarthNow","somewhereBeautiful","guideTitle","guideIntro"]){console.assert(h.includes('data-i18n="'+key+'"'),"missing i18n binding "+key);for(const {code} of supportedLanguages)console.assert(t(key,code)!==key&&String(t(key,code)).trim(),code+" missing "+key)}console.log("ERN core landing localization checks passed");
