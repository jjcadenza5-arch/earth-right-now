import fs from "node:fs";const a=fs.readFileSync("src/app.js","utf8");console.assert(a.includes("const now=new Date();const choice=placeChoice(p,list,{now});"),"place drawer must capture one render moment");console.assert(a.includes('windowTileView(s,i,{now,onOpen:x=>sourceAction(x,{surface:"place-windows"})})'),"place drawer tiles must share the place render moment");console.assert(a.includes("windowStrip(primary.length?primary:all,{limit:8,now})"),"home Choose a Window ranking must share one render moment");console.log("ERN Choose a Window render-clock checks passed");