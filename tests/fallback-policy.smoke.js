import { fallbackChain,nextFallback } from "../src/fallback-chain.js";const bad={health:"OFFLINE",permission:"EMBED_ALLOWED",playback:"EMBED",embedUrl:"https://evil.example/x",sourceUrl:"javascript:alert(1)"};const x=fallbackChain(bad);console.assert(x.length===1&&x[0].mode==="UNAVAILABLE");console.assert(nextFallback(x,"MISSING").mode==="UNAVAILABLE");console.log("ERN fallback policy smoke checks passed");