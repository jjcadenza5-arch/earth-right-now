import assert from "node:assert/strict";import { interpretEarthIntent } from "../src/earth-intent.js";import { sourceMatchesEarthIntents } from "../src/earth-intent-match.js";for(const q of ["somewhere local","a small place","hidden gem","lesser known","off the beaten path"])assert.ok(interpretEarthIntent(q).intents.includes("local"),q);assert.equal(sourceMatchesEarthIntents({title:"Village market",story:"A local small town square"},["local"]),true);assert.equal(sourceMatchesEarthIntents({title:"Iconic skyline",story:"World famous major city landmark"},["local"]),false);console.log("ERN local-place intent passed");
