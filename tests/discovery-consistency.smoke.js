import { searchEarth } from "../src/search-engine.js";import { surprisePool } from "../src/random-earth.js";import { currentSource } from "../src/discovery-eligibility.js";const now=new Date().toISOString(),ok={id:"ok",title:"Beach",health:"HEALTHY",truth:"EXTERNAL_LIVE",permission:"LINK_ONLY",playback:"EXTERNAL",sourceUrl:"https://example.test",lastSuccessfulCheck:now},off={...ok,id:"off",health:"OFFLINE"},unknown={...ok,id:"unknown",health:"UNKNOWN"};console.assert(searchEarth([off,ok],"beach").length===1);console.assert(currentSource(ok)&&!currentSource(unknown));console.assert(surprisePool([unknown,ok])[0].id==="ok");console.log("ERN discovery consistency smoke checks passed");