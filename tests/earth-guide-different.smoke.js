import {differentFrom} from "../src/earth-guide-actions.js";const current={id:"a",placeId:"p1",country:"TH",region:"North"};const rows=[current,{id:"b",placeId:"p2",country:"TH",region:"North"},{id:"c",placeId:"p3",country:"JP",region:"Chubu"}];console.assert(differentFrom(rows,current,{limit:2}).map(x=>x.id).join(",")==="c","prefer genuinely far destinations");console.assert(differentFrom([current,rows[1]],current,{limit:2}).map(x=>x.id).join(",")==="b","fallback must still exclude current place");console.log("ERN Earth Guide different-place checks passed");