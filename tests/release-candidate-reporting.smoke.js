import fs from "node:fs";const script=fs.readFileSync("scripts/release-candidate.mjs","utf8");console.assert(script.includes("publicationReadyForCandidate"),"candidate report must retain publication gate");console.assert(script.includes("candidateBlockers"),"candidate report must expose blockers");console.assert(!script.includes("process.exitCode=1"),"beta CI candidate posture must remain report-only");console.log("release candidate report-only boundary checks passed");