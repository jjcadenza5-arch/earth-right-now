import { validateSubmission } from "../src/business-submission.js";console.assert(validateSubmission({businessName:"Resort",placeName:"Beach",sourceUrl:"https://example.test/cam",rightsConfirmed:true}).length===0);console.assert(validateSubmission({businessName:"",placeName:"Beach",sourceUrl:"bad",rightsConfirmed:false}).length===3);console.log("ERN submission smoke checks passed");