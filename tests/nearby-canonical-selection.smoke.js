import { nearbyDestinations } from "../src/nearby-destinations.js";import { nearbyDestinationViewModel } from "../src/nearby-destination-view-model.js";const origin={id:"a",lat:0,lon:0},places=[origin,{id:"b",lat:0,lon:.01},{id:"c",lat:0,lon:.02}];const raw=nearbyDestinations(origin,places,{limit:1,maxKm:5}),view=nearbyDestinationViewModel(origin,places,{limit:1,maxKm:5});console.assert(raw.length===view.length&&raw[0].place.id===view[0].place.id,"nearby UI must use canonical nearby selection");console.assert(/away$/.test(view[0].distanceLabel),"nearby UI should add only presentation copy");console.log("ERN nearby destination canonical-selection checks passed");