import fs from "node:fs";\nimport assert from "node:assert/strict";\n\nconst src=fs.readFileSync("scripts/build-operator-review.mjs","utf8");\nassert.match(src,/Alternate provider tests/);\nassert.match(src,/failed primary does not require another code\\/deploy cycle/);\nassert.match(src,/researchAlternates\\.map\\(x=>card\\(x,"research"\\)\\)/);\nassert.ok(!src.includes("researchAlternates.map(x=>`<li>"));\nconsole.log("Operator review alternates remain collapsed but are directly testable without redeploy");\n