import { resumeOffer } from "../src/resume-offer.js";const ok={ok:true,data:{placeId:"p",sourceId:"s",surface:"place-windows"}};const x=resumeOffer(ok,{validPlaceIds:new Set(["p"]),validSourceIds:new Set(["s"])});console.assert(x.show&&x.label==="Resume last window"&&x.sourceId==="s");console.assert(!resumeOffer(ok,{validPlaceIds:new Set(),validSourceIds:new Set()}).show);console.assert(!resumeOffer({ok:false,reason:"EXPIRED"}).show);console.log("ERN resume offer smoke checks passed");
