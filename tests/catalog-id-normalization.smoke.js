import { guardCatalog } from "../src/catalog-guard.js";import { validateSource } from "../src/source-validator.js";const base={placeId:"place",title:"X",truth:"EXTERNAL_LIVE",permission:"LINK_ONLY",health:"UNKNOWN",playback:"EXTERNAL",sourceUrl:"https://example.com"};console.assert(validateSource({...base,id:" Bad ID "}).some(x=>x.includes("id")));const r=guardCatalog([{...base,id:"cam"},{...base,id:"CAM",sourceUrl:"https://other.example.com"}]);console.assert(r.valid.length===1&&r.rejected.length===1);console.log("ERN catalog ID normalization smoke checks passed");