import fs from "node:fs";const rows=JSON.parse(fs.readFileSync("data/sources.json","utf8"));const missing=rows.filter(x=>!String(x.story||"").trim());console.assert(missing.length===0,"all active sources should carry visitor-facing story copy");for(const id of ["maui-hale-pau-hana","waikiki-south-shore","cold-lake-marina"]){const x=rows.find(y=>y.id===id);console.assert(x.health==="DEGRADED"&&/reverified/.test(x.story),"degraded story must preserve uncertainty");}console.log("ERN catalog story completeness passed");
