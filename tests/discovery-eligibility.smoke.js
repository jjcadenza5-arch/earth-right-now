import { discoveryPool } from "../src/discovery-eligibility.js";const rows=[{id:"off",health:"OFFLINE",permission:"LINK_ONLY",playback:"EXTERNAL",sourceUrl:"https://example.test"},{id:"ok",health:"UNKNOWN",permission:"LINK_ONLY",playback:"EXTERNAL",sourceUrl:"https://example.test"}];const x=discoveryPool(rows);console.assert(x.length===1&&x[0].id==="ok");console.log("ERN discovery eligibility smoke checks passed");