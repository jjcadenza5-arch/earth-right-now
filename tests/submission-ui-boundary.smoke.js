import fs from "node:fs";const html=fs.readFileSync("index.html","utf8");console.assert(html.includes(">Prepare for review<"),"submission CTA must not imply delivery while transport is disabled");console.assert(html.includes("does not send or publish a camera"),"submission UI must disclose offline preparation boundary");console.assert(!html.includes(">Submit for review<"),"disabled transport must not present a submit/send CTA");console.log("ERN submission UI transport-boundary checks passed");