import { interpretEarthIntent } from "../src/earth-intent.js";const thai=interpretEarthIntent("เชียงใหม่ ตอนนี้");console.assert(thai.tokens.includes("ตอนนี้"),"Thai combining marks must survive normalization");console.assert(thai.wantsCurrent&&thai.currentTerms.includes("ตอนนี้"),"Thai current intent must survive normalization");const ko=interpretEarthIntent("서울 지금");console.assert(ko.tokens.includes("지금")&&ko.wantsCurrent,"Korean current intent must survive normalization");console.assert(!interpretEarthIntent("Brighton").wantsCurrent,"Latin place names must remain ordinary");console.log("ERN Unicode normalization checks passed");