import { interpretEarthIntent,normalizeEarthText } from "../src/earth-intent.js";const thai=interpretEarthIntent("เชียงใหม่ ตอนนี้");console.assert(thai.tokens.includes("ตอนนี้"),"Thai marks must survive normalization");console.assert(thai.wantsCurrent&&thai.currentTerms.includes("ตอนนี้"),"Thai current intent must survive normalization");console.assert(normalizeEarthText("서울 지금")==="서울 지금","non-launch scripts must still survive normalization even without translated intent vocabulary");console.assert(!interpretEarthIntent("Brighton").wantsCurrent,"Latin place names must remain ordinary");console.log("ERN Unicode normalization checks passed");