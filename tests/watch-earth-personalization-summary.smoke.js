import assert from "node:assert/strict";import { watchEarthPersonalizationSummary as s } from "../src/watch-earth-personalization-summary.js";assert.equal(s().active,false);assert.equal(s().copy,"Global beautiful-now curation");const x=s({favoriteSourceIds:["a"],favoritePlaceIds:new Set(["p"]),taste:{signals:3}});assert.equal(x.active,true);assert.equal(x.savedWindows,1);assert.equal(x.savedPlaces,1);assert.equal(x.signals,3);assert.match(x.copy,/discovery stays open/);assert.match(x.privacy,/browser/);console.log("ERN Watch Earth personalization summary passed");
