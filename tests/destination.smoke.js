import { destinationSummary,destinationRank } from "../src/destination-engine.js";const p={sources:[{health:"HEALTHY",truth:"LIVE_VIDEO",quality:90,moment:80,freshness:70},{health:"UNKNOWN",truth:"LIVE_IMAGE",quality:75,moment:60,freshness:null}]};const s=destinationSummary(p);console.assert(s.windows===2&&s.hasChoice===true);console.assert(destinationRank(p)>0);console.log("ERN destination smoke checks passed");