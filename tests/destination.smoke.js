import { destinationSummary,destinationRank } from "../src/destination-engine.js";const now=new Date().toISOString(),p={sources:[{id:"a",health:"HEALTHY",truth:"EXTERNAL_LIVE",permission:"LINK_ONLY",playback:"EXTERNAL",sourceUrl:"https://example.test/a",checkedAt:now,lastSuccessfulCheck:now,quality:90,moment:80,freshness:70},{id:"b",health:"UNKNOWN",truth:"LIVE_IMAGE",permission:"LINK_ONLY",playback:"EXTERNAL",sourceUrl:"https://example.test/b",quality:75,moment:60,freshness:null}]};const s=destinationSummary(p);console.assert(s.windows===2&&s.totalWindows===2&&s.hasChoice===true&&s.current===1);console.assert(destinationRank(p)>0);console.log("ERN destination smoke checks passed");