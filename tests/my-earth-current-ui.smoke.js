import fs from "node:fs";const a=fs.readFileSync("src/app.js","utf8"),h=fs.readFileSync("index.html","utf8");for(const x of ["const now=new Date(),m=buildMyEarth({sources:all,places},{now})","myEarthCurrentCopy(v)",'id="myEarthCurrentNote"'])console.assert((a+h).includes(x),"My Earth current snapshot UI must include "+x);console.log("ERN My Earth current snapshot UI checks passed");