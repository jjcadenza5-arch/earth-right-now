import { createWindowStripController } from "../src/window-strip-controller.js";const ok={id:"ok",health:"HEALTHY",permission:"LINK_ONLY",playback:"EXTERNAL",sourceUrl:"https://example.test"},bad={id:"bad",health:"OFFLINE",permission:"LINK_ONLY",playback:"EXTERNAL",sourceUrl:"https://example.test"};const x=createWindowStripController({sources:[bad,ok]});console.assert(x.items().length===1&&x.selected()==="ok"&&!x.hasChoice());console.assert(x.select("bad")===null);console.log("ERN window-strip controller smoke checks passed");