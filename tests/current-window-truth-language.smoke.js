import { currentWindowEyebrow,currentWindowAction } from "../src/current-window-label.js";const now=new Date("2026-09-20T10:00:00Z"),base={health:"HEALTHY",permission:"LINK_ONLY",playback:"EXTERNAL",checkedAt:"2026-09-20T09:59:00Z",lastSuccessfulCheck:"2026-09-20T09:59:00Z",sourceUrl:"https://example.test"};const live={...base,truth:"EXTERNAL_LIVE"};const image={...base,truth:"LIVE_IMAGE"};console.assert(currentWindowEyebrow(live,{now})==="NEAR-NOW AT SOURCE");console.assert(currentWindowAction(live,{now})==="Open near-now source");console.assert(currentWindowEyebrow(image,{now})!=="NEAR-NOW AT SOURCE","unverified image freshness must not become near-now");console.log("ERN current-window truth-language checks passed");