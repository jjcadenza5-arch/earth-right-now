import fs from "node:fs";const p=JSON.parse(fs.readFileSync("package.json","utf8")),s=fs.readFileSync("scripts/domain-health.mjs","utf8"),w=fs.readFileSync(".github/workflows/domain-health.yml","utf8");console.assert(p.scripts["domain:health"]==="node scripts/domain-health.mjs");console.assert(s.includes("TLS_HOSTNAME_MISMATCH"));console.assert(s.includes("subjectAltName"));console.assert(w.includes('cron: "17 */6 * * *"'));console.assert(w.includes("www.earthrightnow.app"));console.assert(w.includes("Enforce apex health"));console.assert(w.match(/set -o pipefail/g)?.length>=2,"domain workflow must preserve diagnostic exit codes through tee");console.log("ERN domain-health diagnostics wiring passed");
