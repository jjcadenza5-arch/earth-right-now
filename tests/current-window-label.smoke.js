import { currentWindowEyebrow,currentWindowAction } from "../src/current-window-label.js";const external={health:"HEALTHY",truth:"EXTERNAL_LIVE",permission:"LINK_ONLY",playback:"EXTERNAL"};console.assert(currentWindowEyebrow(external)==="CURRENT AT SOURCE");console.assert(currentWindowAction(external)==="Open current source");const image={health:"HEALTHY",truth:"LIVE_IMAGE",permission:"EMBED_ALLOWED",playback:"IMAGE_REFRESH"};console.assert(currentWindowEyebrow(image)==="CURRENT IMAGE");console.log("ERN current window labels smoke checks passed");