import { currentWindowEyebrow,currentWindowAction } from "../src/current-window-label.js";import { sourceBadges } from "../src/source-badges.js";const now=new Date("2026-09-20T08:00:00Z"),base={truth:"LIVE_IMAGE",playback:"EXTERNAL",permission:"LINK_ONLY",health:"HEALTHY",sourceUrl:"https://example.com/cam",checkedAt:"2026-09-20",lastSuccessfulCheck:"2026-09-20"};console.assert(currentWindowEyebrow(base,{now})==="IMAGE SOURCE");console.assert(currentWindowAction(base,{now})==="Open source");console.assert(sourceBadges(base,{now}).includes("IMAGE SOURCE · FRESHNESS UNVERIFIED"));const fresh={...base,freshnessEvidence:"Provider documents refreshed camera imagery"};console.assert(currentWindowEyebrow(fresh,{now})==="NEAR-NOW IMAGE");console.assert(sourceBadges(fresh,{now}).includes("REFRESHED IMAGE · NEAR NOW"));console.log("ERN compact image-truth language checks passed");