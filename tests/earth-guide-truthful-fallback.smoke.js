import { earthGuideReply } from "../src/earth-guide.js";const x=earthGuideReply({query:"live beach now",empty:true,count:0});console.assert(x.tone==="EMPTY");console.assert(x.text.includes("without “live”"),"empty live search should tell the visitor how to widen safely");console.assert(x.text.includes("reference images"),"fallback must preserve reference-view truth");console.log("ERN Earth Guide truthful fallback checks passed");