import fs from "node:fs";const a=fs.readFileSync("src/app.js","utf8");for(const x of ["function runERN(){const now=new Date()","discoveryResult(all,query,{limit:6,mode:\"ai\",taste,now})","runtimeHealthySources(all,now)","currentDiscoveryPool(healthy,{now})","const now=new Date(),result=discoveryResult(all,e.target.value,{limit:12,now})"])console.assert(a.includes(x),"Search Earth surfaces must share one query moment: "+x);console.log("ERN Search Earth query-clock checks passed");