import fs from "node:fs";const h=fs.readFileSync(new URL("../index.html",import.meta.url),"utf8");console.assert(h.includes("Watch Earth for beautiful moments happening now, or search any place before you go."),"homepage must explain Watch Earth vs Search Earth");console.assert(h.includes("SEARCH EARTH"),"search surface should be explicitly named");console.assert(h.includes("live/current first, or a clearly labeled reference image when needed"),"search promise must state truthful fallback hierarchy");console.assert(!h.includes('<h2 id="atlasClusterTitle">Earth windows</h2>'),"Atlas drawer should use neutral views language");console.log("ERN homepage product-model copy checks passed");