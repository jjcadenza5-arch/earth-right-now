import { validateSubmission,submissionRecord } from "../src/business-submission.js";const good={businessName:" Example Resort ",placeName:"Beach",sourceUrl:"https://example.com/cam",contact:"cam@example.com",rightsConfirmed:true};console.assert(validateSubmission(good).length===0);console.assert(validateSubmission({...good,sourceUrl:"javascript:alert(1)"}).length>0);console.assert(validateSubmission({...good,sourceUrl:"http://localhost/cam"}).length>0);console.assert(validateSubmission({...good,contact:"bad"}).length>0);const r=submissionRecord(good,{now:()=>"2026-09-19T00:00:00Z"});console.assert(r.ok&&r.record.status==="PENDING_REVIEW"&&r.record.businessName==="Example Resort");console.log("ERN business submission smoke checks passed");