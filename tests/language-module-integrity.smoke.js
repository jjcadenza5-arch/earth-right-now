import { languageOptions,t } from "../src/language.js";const codes=languageOptions().map(x=>x.code);console.assert(codes.join(",")==="en,th,de,fr,es,ja,zh","language module must parse with the intended seven UI languages");for(const code of codes)console.assert(t("watchEarth",code)&&t("search",code),code+" should expose core navigation copy");console.log("ERN language module syntax and coverage checks passed");