import { clusterSources } from "../src/atlas-cluster.js";import { buildAtlas } from "../src/atlas-model.js";const a={id:"a",lat:10,lon:20,country:"X"},b={id:"b",lat:11,lon:21,country:"X"},c={id:"c",lat:null,lon:null,country:"Y"};const clusters=clusterSources([a,b,c]);console.assert(clusters.length===1&&clusters[0].sources.length===2&&clusters[0].count===2);const model=buildAtlas([a,b,c]);console.assert(model.mappedCount===2&&model.unmappedCount===1&&model.clusters[0].sources.length===2);console.log("ERN Atlas cluster contract smoke checks passed");