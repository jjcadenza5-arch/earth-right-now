import { posterFor,posterTruth } from "../src/source-poster.js";const p=posterFor({title:"Beach",thumbnailUrl:"https://images.example/beach.jpg",truth:"LIVE_VIDEO"});console.assert(p.kind==="image"&&!p.current,"a thumbnail must never inherit live/current status from its source");console.assert(posterTruth(p).label==="ILLUSTRATIVE VISUAL","poster must be labeled conceptually as illustrative, not current evidence");const g=posterFor({title:"Mountain"});console.assert(!g.current&&posterTruth(g).current===false,"generated fallback must never count as current evidence");console.log("ERN poster truth-boundary checks passed");