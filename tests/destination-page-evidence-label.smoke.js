import fs from "node:fs";const s=fs.readFileSync("scripts/build-destination-pages.mjs","utf8");console.assert(s.includes('currentWindowEyebrow'),"destination pages must reuse runtime evidence labels");console.assert(!s.includes('String(s.truth||"").replaceAll'),"raw truth type must not be presented as current evidence");console.log("ERN destination page evidence-label checks passed");
