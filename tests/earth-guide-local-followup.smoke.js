import assert from "node:assert/strict";import { earthGuideFollowUps } from "../src/earth-guide.js";const prompts=earthGuideFollowUps({count:3,query:"live beach"});assert.ok(prompts.length<=3);const broad=earthGuideFollowUps({count:3,query:"beautiful night"});assert.ok(broad.some(x=>x==="Take me somewhere small and local")||broad.some(x=>x==="Show me somewhere completely different"));console.log("ERN Earth Guide discovery follow-up passed");
