import fs from "node:fs";const s=fs.readFileSync("src/travel-bridge-view.js","utf8");for(const x of ["travelOfferView(offer,{now=Date.now()}={})","currentTravelOffer(offer,{now})","travelBridgeView(place,{onIntent,offers=[],now=Date.now()}={})","currentTravelOffer(o,{now})","travelOfferView(o,{now})"])console.assert(s.includes(x),"travel bridge must share one verification moment: "+x);console.log("ERN travel bridge single-clock checks passed");