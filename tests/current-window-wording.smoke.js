import { currentWindowEyebrow,currentWindowAction } from "../src/current-window-label.js";const now=new Date().toISOString(),base={permission:"LINK_ONLY",health:"HEALTHY",playback:"EXTERNAL",truth:"EXTERNAL_LIVE",sourceUrl:"https://example.com",checkedAt:now,lastSuccessfulCheck:now};console.assert(currentWindowEyebrow(base)==="NEAR-NOW AT SOURCE");console.assert(currentWindowAction(base)==="Open near-now source");const stale={...base,checkedAt:"2020-01-01",lastSuccessfulCheck:"2020-01-01"};console.assert(currentWindowEyebrow(stale)==="SOURCE");console.assert(currentWindowAction(stale)==="Open source");console.log("ERN current-window wording smoke checks passed");