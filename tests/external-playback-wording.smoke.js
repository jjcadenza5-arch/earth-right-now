import { readFileSync } from "node:fs";const src=readFileSync(new URL("../src/playback-adapters.js",import.meta.url),"utf8");console.assert(!src.includes("preserves its verified current-source status"));console.assert(src.includes("not presenting it as current"));console.assert(src.includes("Current view at source"));console.log("ERN external playback wording smoke checks passed");