import { sourceBadges } from "../src/source-badges.js";const now=new Date().toISOString(),base={health:"HEALTHY",permission:"LINK_ONLY",playback:"EXTERNAL",truth:"LIVE_IMAGE",sourceUrl:"https://example.com",checkedAt:now,lastSuccessfulCheck:now,freshnessEvidence:"Provider documents refreshed camera imagery"};console.assert(sourceBadges(base).includes("REFRESHED IMAGE · NEAR NOW"));const stale={...base,checkedAt:"2020-01-01",lastSuccessfulCheck:"2020-01-01"};console.assert(!sourceBadges(stale).includes("REFRESHED IMAGE · NEAR NOW"));console.log("ERN source badge currentness smoke checks passed");