import { destinationSummary } from "../src/destination-engine.js";const now=new Date().toISOString(),base={permission:"LINK_ONLY",health:"HEALTHY",playback:"EXTERNAL",truth:"EXTERNAL_LIVE",sourceUrl:"https://example.com",checkedAt:now,lastSuccessfulCheck:now};const stale={...base,checkedAt:"2020-01-01",lastSuccessfulCheck:"2020-01-01"};const x=destinationSummary({sources:[stale]});console.assert(x.windows===1);console.assert(x.current===0);console.log("ERN destination semantics smoke checks passed");