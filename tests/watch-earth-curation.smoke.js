import { diverseCuration } from "../src/curation.js";const now=new Date().toISOString(),base={truth:"EXTERNAL_LIVE",permission:"LINK_ONLY",health:"HEALTHY",playback:"EXTERNAL",sourceUrl:"https://example.com",checkedAt:now,lastSuccessfulCheck:now,quality:80,freshness:80,moment:80};const rows=[{...base,id:"a",country:"A"},{...base,id:"b",country:"A"},{...base,id:"c",country:"A"},{...base,id:"d",country:"B"},{...base,id:"stale",country:"C",checkedAt:"2020-01-01",lastSuccessfulCheck:"2020-01-01"}];const r=diverseCuration(rows,{limit:10,requireCurrent:true});console.assert(r.length===3);console.assert(!r.some(x=>x.id==="stale"));console.assert(r.filter(x=>x.country==="A").length===2);console.log("ERN Watch Earth current curation smoke checks passed");