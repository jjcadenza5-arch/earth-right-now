import { curate } from "../src/curation.js";const now=new Date().toISOString(),base={permission:"LINK_ONLY",playback:"EXTERNAL",sourceUrl:"https://example.test/live",checkedAt:now,lastSuccessfulCheck:now};const rows=[{...base,id:"a",health:"HEALTHY",truth:"LIVE_VIDEO",quality:90,freshness:90,moment:90},{...base,id:"b",health:"UNKNOWN",truth:"LIVE_VIDEO",quality:100,freshness:100,moment:100},{...base,id:"c",health:"HEALTHY",truth:"PREVIEW",playback:"PREVIEW",quality:100}];const out=curate(rows);console.assert(out.length===1&&out[0].id==="a");console.log("ERN curation smoke checks passed");