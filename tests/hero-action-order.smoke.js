import { readFileSync } from "node:fs";const html=readFileSync(new URL("../index.html",import.meta.url),"utf8");const order=["watch-live","watch-earth","my-earth","explore","next-live","surprise"].map(x=>html.indexOf('data-action="'+x+'"'));console.assert(order.every(x=>x>=0));console.assert(order.every((x,i)=>i===0||x>order[i-1]));console.assert(html.includes('data-action="surprise" class="hero-secondary-action"'));console.log("ERN Hero action order smoke checks passed");