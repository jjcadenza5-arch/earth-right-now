import fs from "node:fs";const v=fs.readFileSync("src/place-memory-view.js","utf8"),m=fs.readFileSync("src/my-earth-model.js","utf8");console.assert(v.includes('text:"View place"')&&!v.includes('text:"Windows"'),"My Earth place action should not imply every destination has live windows");console.assert(m.includes("currentSource(s,{now})"),"My Earth current favorites must use canonical currentness");console.log("ERN My Earth destination-first truth checks passed");