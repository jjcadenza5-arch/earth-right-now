import { playbackCapability } from "../src/playback-capability.js";const now=new Date().toISOString(),base={permission:"LINK_ONLY",health:"HEALTHY",playback:"EXTERNAL",truth:"EXTERNAL_LIVE",sourceUrl:"https://example.com",checkedAt:now,lastSuccessfulCheck:now};console.assert(playbackCapability(base).label==="Open near-now source");console.assert(playbackCapability({...base,checkedAt:"2020-01-01",lastSuccessfulCheck:"2020-01-01"}).label==="Open source");console.log("ERN playback currentness smoke checks passed");