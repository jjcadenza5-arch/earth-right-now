import { createPlayerSession } from "../src/player-session.js";const s=createPlayerSession();let cleaned=0;s.begin({id:"a"},()=>()=>cleaned++);const g1=s.state().generation;s.begin({id:"b"},()=>()=>cleaned++);console.assert(cleaned===1);console.assert(s.state().sourceId==="b");console.assert(s.state().generation>g1);s.end();console.assert(cleaned===2&&!s.state().active);console.log("ERN player session generation smoke checks passed");