import assert from "node:assert/strict";import fs from "node:fs";const rows=JSON.parse(fs.readFileSync("data/sources.json","utf8"));for(const id of ["coogee-randwick-beaches","boston-harbor-islands","diano-marina","farm-tomita","waikiki-beach","sottomarina-chioggia"]){const s=rows.find(x=>x.id===id);assert.ok(s,id);assert.equal(s.permission,"LINK_ONLY");assert.equal(s.playback,"EXTERNAL");assert.equal(s.health,"HEALTHY");assert.match(s.rightsBasis,/Current official/);assert.ok(s.checkedAt&&s.lastSuccessfulCheck)}const j=rows.find(x=>x.id==="jungfrau-region");assert.equal(j.health,"DEGRADED");assert.equal(j.permission,"LINK_ONLY");assert.equal(j.lastSuccessfulCheck,null);assert.match(j.failureReason,/offline/i);console.log("ERN official external recovery tranche checks passed");
