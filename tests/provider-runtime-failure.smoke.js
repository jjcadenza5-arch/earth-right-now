import { providerRuntimeFailure,shouldOfferExternalFallback } from "../src/provider-runtime-failure.js";const blocked=providerRuntimeFailure({mode:"EMBED",httpStatus:403});console.assert(blocked.kind==="PROVIDER_BLOCKED"&&!blocked.definitive);console.assert(shouldOfferExternalFallback({id:"x"},blocked));const missing=providerRuntimeFailure({mode:"IMAGE_REFRESH",httpStatus:404});console.assert(missing.kind==="MEDIA_MISSING"&&missing.definitive);console.log("ERN provider runtime failure smoke checks passed");