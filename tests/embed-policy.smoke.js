import { allowedEmbedUrl } from "../src/embed-policy.js";console.assert(allowedEmbedUrl("https://couchtourist.com/embed/cam/604/"));console.assert(allowedEmbedUrl("https://www.youtube.com/embed/example"));console.assert(!allowedEmbedUrl("http://couchtourist.com/embed/cam/604/"));console.assert(!allowedEmbedUrl("https://evil.example/embed"));console.assert(!allowedEmbedUrl("javascript:alert(1)"));console.log("ERN embed policy smoke checks passed");