import { explicitImageFreshness,nowEvidenceClass } from "../src/now-evidence.js";const base={truth:"LIVE_IMAGE",playback:"EXTERNAL",health:"HEALTHY",permission:"LINK_ONLY",sourceUrl:"https://example.com/cam",lastSuccessfulCheck:"2026-09-20",checkedAt:"2026-09-20"};console.assert(!explicitImageFreshness(base),"a recent source check must not prove the image frame is fresh");console.assert(nowEvidenceClass(base,{now:new Date("2026-09-20T08:00:00Z")}).label==="IMAGE SOURCE","unproven image freshness should stay conservative");const evidenced={...base,freshnessEvidence:"Provider documents regularly refreshed webcam imagery"};console.assert(explicitImageFreshness(evidenced));console.assert(nowEvidenceClass(evidenced,{now:new Date("2026-09-20T08:00:00Z")}).nearNow,"explicit refresh evidence may qualify as near-now");console.log("ERN image freshness evidence checks passed");