import { buildAtlas } from "../src/atlas-model.js";const now=new Date().toISOString(),mk=(id,lat,lon)=>({id,lat,lon,country:"X",categories:["Wildlife"],health:"HEALTHY",truth:"EXTERNAL_LIVE",permission:"LINK_ONLY",playback:"EXTERNAL",sourceUrl:"https://example.test/"+id,checkedAt:now,lastSuccessfulCheck:now});const a=buildAtlas([mk("a",1,1),mk("b",1.1,1.1)],{});console.assert(a.count===2&&a.clusters.length>=1);console.log("ERN Atlas model smoke checks passed");