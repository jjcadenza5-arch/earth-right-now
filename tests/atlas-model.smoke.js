import { buildAtlas } from "../src/atlas-model.js";const rows=[{id:"a",lat:1,lon:1,country:"X",categories:["Wildlife"],health:"HEALTHY",truth:"LIVE_VIDEO"},{id:"b",lat:1.1,lon:1.1,country:"X",categories:["Wildlife"],health:"HEALTHY",truth:"LIVE_VIDEO"}];const a=buildAtlas(rows,{});console.assert(a.count===2&&a.clusters.length>=1);console.log("ERN Atlas model smoke checks passed");