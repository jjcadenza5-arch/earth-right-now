import fs from "node:fs";const s=fs.readFileSync("src/destination-card-view.js","utf8");console.assert(s.includes('" available "+(x.windows===1?"view":"views")'),"destination cards should describe mixed evidence as views");console.assert(s.includes('"No available views"'),"destination empty copy should remain fallback-neutral");console.assert(!s.includes('"No available windows"'),"destination fallback must not imply a camera window");console.log("ERN destination-card neutral-view copy checks passed");