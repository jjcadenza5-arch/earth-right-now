import { PlaybackController } from "../src/playback-controller.js";const mk=x=>new PlaybackController({registry:new Map([[x.id,x]])});const bad={id:"bad",health:"HEALTHY",truth:"LIVE_VIDEO",permission:"EMBED_ALLOWED",playback:"EMBED",embedUrl:"https://evil.example/embed"};console.assert(mk(bad).resolveMode(bad)==="EXTERNAL"||mk(bad).resolveMode(bad)==="UNAVAILABLE");const ok={...bad,id:"ok",embedUrl:"https://couchtourist.com/embed/cam/604/"};console.assert(mk(ok).resolveMode(ok)==="EMBED");console.log("ERN playback controller policy smoke checks passed");