import { normalizeRecovered } from "../src/catalog-import.js";import { validateSource } from "../src/source-validator.js";const x=normalizeRecovered({id:"x",title:"Missing URL"});console.assert(x.sourceUrl===null);console.assert(validateSource(x).some(e=>e.includes("sourceUrl")));console.assert(!JSON.stringify(x).includes("about:blank"));console.log("ERN recovery quarantine smoke checks passed");