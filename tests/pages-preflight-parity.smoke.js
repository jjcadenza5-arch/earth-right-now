import fs from "node:fs";const w=fs.readFileSync(".github/workflows/pages.yml","utf8");for(const cmd of ["npm run accessibility:preflight","npm run mobile:preflight","npm run release:preflight"])console.assert(w.includes(cmd),`Pages verify missing ${cmd}`);console.assert(w.indexOf("npm run release:preflight")<w.indexOf("npm run release:build"),"preflights must run before artifact build");console.log("ERN Pages preflight parity checks passed");
