import { parsePlaceHash,safePlaceFromHash } from "../src/place-routing.js";console.assert(parsePlaceHash("#place=hello%20earth")==="hello earth");console.assert(parsePlaceHash("#place=%E0%A4%A")===null);console.assert(safePlaceFromHash("#place=x",[{id:"x"}])?.id==="x");console.assert(safePlaceFromHash("#place=missing",[{id:"x"}])===null);console.log("ERN safe place routing smoke checks passed");