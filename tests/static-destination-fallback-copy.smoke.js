import fs from "node:fs";const s=fs.readFileSync(new URL("../scripts/build-destination-pages.mjs",import.meta.url),"utf8");console.assert(!s.includes("Current camera windows for "),"static destination fallback copy must not promise current cameras");console.assert(s.includes("currentWindowEyebrow"),"static destination pages must use shared evidence labels, including REFERENCE IMAGE");console.assert(s.includes("Reference images are never presented as live."),"static destination disclosure must protect fallback truth");console.log("ERN static destination fallback-language checks passed");
