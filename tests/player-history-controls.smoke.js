import { playerStateView } from "../src/player-state-view.js";let v=playerStateView({open:true,history:["a","b","c"],cursor:1},{});console.assert(v.canPrevious&&v.canNext);v=playerStateView({open:true,history:["a","b","c"],cursor:0},{});console.assert(!v.canPrevious&&v.canNext);v=playerStateView({open:true,history:["a","b","c"],cursor:2},{});console.assert(v.canPrevious&&!v.canNext);console.log("ERN player history controls smoke checks passed");