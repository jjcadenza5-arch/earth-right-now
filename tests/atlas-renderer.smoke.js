import { renderAtlas } from "../src/atlas-renderer.js";const mount=document.createElement("div");renderAtlas({mount,clusters:[{sources:[{id:"a",title:"A",lat:0,lon:0}]}]});console.assert(mount.querySelectorAll(".atlas-pin").length===1);console.log("ERN Atlas renderer smoke checks passed");