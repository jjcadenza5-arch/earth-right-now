import "./test-browser-env.mjs";import { renderAtlas } from "../src/atlas-renderer.js";const mount=document.createElement("div");renderAtlas({mount,clusters:[{sources:[{id:"a",title:"A",lat:0,lon:0,coordinateBasis:"PLACE_REFERENCE"}]}]});const pin=mount.querySelector(".atlas-pin");console.assert(pin);console.assert(pin.title.includes("Place reference point"));console.assert(pin.title.includes("not necessarily the exact camera position"));console.log("ERN Atlas renderer smoke checks passed");