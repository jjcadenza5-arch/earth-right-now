import assert from "node:assert/strict";import { languageAudit,languageLabel } from "../src/language-audit.js";import { t,hasTranslation,supportedLanguages } from "../src/language.js";const a=languageAudit(t,hasTranslation);assert.equal(a.ok,true);assert.deepEqual(Object.keys(a.missing),supportedLanguages.map(x=>x.code));for(const x of supportedLanguages)assert.equal(languageLabel(x.code),x.label);console.log("ERN all-language audit checks passed");
