import fs from "node:fs";const app=fs.readFileSync(new URL("../src/app.js",import.meta.url),"utf8");console.assert(app.includes("Submission prepared locally for review. It has not been sent or published."),"prepared submission status must disclose that transport is offline");console.assert(!app.includes("Submission prepared for review. No camera is published automatically."),"ambiguous legacy prepared status must not return");console.log("ERN submission prepared-state copy checks passed");