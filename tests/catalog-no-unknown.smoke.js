import { readFileSync } from "node:fs";const rows=JSON.parse(readFileSync(new URL("../data/sources.json",import.meta.url),"utf8"));console.assert(rows.length===62);console.assert(rows.filter(x=>x.health==="UNKNOWN").length===0);console.assert(rows.filter(x=>x.health==="HEALTHY").length>=59);console.assert(rows.filter(x=>x.health==="DEGRADED").length===3);for(const id of ["pattaya-city-live","glenelg-sa","brighton-sa"]){const s=rows.find(x=>x.id===id);console.assert(s&&s.permission==="LINK_ONLY"&&s.playback==="EXTERNAL")}console.log("ERN public catalog UNKNOWN-source smoke checks passed");