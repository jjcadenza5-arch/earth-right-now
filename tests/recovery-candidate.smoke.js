import { normalizeRecoveryCandidate,candidateEligibleForPublicRegistry } from "../src/recovery-candidate.js";const x=normalizeRecoveryCandidate({id:"a",health:"HEALTHY",permission:"EMBED_ALLOWED",playback:"EMBED",embedUrl:"https://example.test/e",sourceUrl:"https://example.test"});console.assert(x.health==="UNKNOWN"&&x.permission==="UNKNOWN"&&x.embedUrl===null&&!candidateEligibleForPublicRegistry(x));console.log("ERN recovery candidate smoke checks passed");