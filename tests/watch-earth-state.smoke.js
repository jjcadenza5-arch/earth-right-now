import { createWatchEarthSession } from "../src/watch-earth-session.js";const calls=[],session=createWatchEarthSession({sources:[{id:"a"},{id:"b"}],player:{play(){}},openSource:(s)=>calls.push(s.id)});session.show(0);session.resume(999999);console.assert(session.state().playing===true);session.pause();console.assert(session.state().playing===false);session.next();console.assert(calls.join(",")==="a,b");console.log("ERN Watch Earth state smoke checks passed");