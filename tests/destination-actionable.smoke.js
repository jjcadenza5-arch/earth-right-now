import { destinationSummary } from "../src/destination-engine.js";const ok={health:"HEALTHY",truth:"LIVE_VIDEO",permission:"EMBED_ALLOWED",playback:"EMBED",embedUrl:"https://couchtourist.com/embed/cam/1/",lastSuccessfulCheck:new Date().toISOString()},bad={...ok,health:"OFFLINE"};const x=destinationSummary({sources:[ok,bad]});console.assert(x.windows===1&&x.totalWindows===2&&!x.hasChoice);console.log("ERN destination actionable-window smoke checks passed");