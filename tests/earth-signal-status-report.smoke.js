import assert from "node:assert/strict";import {EARTH_SIGNAL_CAPABILITIES} from "../src/earth-signal-capabilities.js";import {earthSignalStatusReport,earthSignalStatusText} from "../src/earth-signal-status-report.js";
const r=earthSignalStatusReport();assert.equal(r.mode,"READ_ONLY");assert.equal(r.ready,false);assert.equal(r.blockers.length,6);assert.equal(r.truth,"Architecture readiness and an API contract are not production capability.");assert.equal(r.backendContract.version,"2026-09-25.v1");assert.equal(r.backendContract.ttlMinutes,45);assert.equal(r.backendFoundation.state,"PREPARED_NOT_DEPLOYED");assert.equal(r.backendFoundation.deployedTransport,false);assert.equal(r.backendFoundation.failClosedHttpAdapter,true);assert.equal(r.backendFoundation.durableStorageContract,true);assert.equal(r.backendFoundation.storageSchemaValidation,true);assert.equal(r.backendFoundation.gatedServiceLayer,true);assert.equal(r.privacyNoticeDraft.contentReady,true);assert.equal(r.privacyNoticeDraft.published,false);assert.equal(r.privacyNoticeDraft.activationSatisfied,false);assert.equal(r.deployment.state,"NOT_DEPLOYED");assert.equal(r.deployment.ready,false);assert.equal(r.deployment.missing.length,10);
assert.match(earthSignalStatusText(),/read-only · 6 infrastructure blockers/);
const all=Object.fromEntries(Object.keys(EARTH_SIGNAL_CAPABILITIES).map(k=>[k,true]));assert.equal(earthSignalStatusReport(all).mode,"CONTRIBUTION_ENABLED");
console.log("Earth Signal operator status checks passed");
