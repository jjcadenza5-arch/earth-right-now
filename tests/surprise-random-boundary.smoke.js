import { surpriseSource } from "../src/random-earth.js";const now=new Date().toISOString(),rows=[0,1].map(i=>({id:String(i),health:"HEALTHY",truth:"EXTERNAL_LIVE",permission:"LINK_ONLY",playback:"EXTERNAL",sourceUrl:"https://example.com/"+i,checkedAt:now,lastSuccessfulCheck:now}));console.assert(surpriseSource(rows,()=>1)?.id);console.assert(surpriseSource(rows,()=>-1)?.id);console.assert(surpriseSource(rows,()=>NaN)?.id);console.log("ERN Surprise boundary smoke checks passed");