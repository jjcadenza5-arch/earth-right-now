import fs from "node:fs";const h=fs.readFileSync("index.html","utf8"),a=fs.readFileSync("src/app.js","utf8");console.assert(h.includes('id="atlasCoverage"'));console.assert(a.includes("model.unmappedCount"));console.assert(a.includes("awaiting map coordinates"));console.assert(a.includes("full spatial coverage for this filter"));console.log("ERN Atlas spatial-coverage disclosure passed");
