import { normalizeEarthText,foldEarthSearchText,interpretEarthIntent,earthIntentHints } from "../src/earth-intent.js";console.assert(typeof interpretEarthIntent==="function"&&typeof earthIntentHints==="function","public intent exports must exist");console.assert(foldEarthSearchText("Kaikōura Coast")==="kaikoura coast","Latin destination folding should remain forgiving");console.assert(normalizeEarthText("เชียงใหม่ ตอนนี้")==="เชียงใหม่ ตอนนี้"&&interpretEarthIntent("เชียงใหม่ ตอนนี้").wantsCurrent,"Thai intent must remain intact");console.log("ERN Earth intent module integrity checks passed");