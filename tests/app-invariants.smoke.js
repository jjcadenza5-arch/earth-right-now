import fs from "node:fs";const c=fs.readFileSync(new URL("../src/app.js",import.meta.url),"utf8");console.assert(c.indexOf("const player=createERNPlayback")<c.indexOf("watchSession=createWatchEarthSession"));console.assert((c.match(/addEventListener\("keydown"/g)||[]).length===1);console.assert(!c.includes('$("#my-earth").scrollIntoView'));console.assert(!c.includes('$("#explore").scrollIntoView'));console.assert(c.includes("loadFavorites()"));console.assert(c.includes("watchSession?.pause()"));console.log("ERN app invariant smoke checks passed");
console.assert(app.includes("freshnessClock.destroy()"),"App lifecycle must destroy the freshness clock and its visibility listener");
