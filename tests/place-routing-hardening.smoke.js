import { parsePlaceHash,safePlaceFromHash } from "../src/place-routing.js";console.assert(parsePlaceHash("#place=abc")==="abc");console.assert(parsePlaceHash("#place=abc%20def")==="abc def");console.assert(parsePlaceHash("#place=")===null);console.assert(parsePlaceHash("#place=abc&x=1")===null);console.assert(parsePlaceHash("#other=abc")===null);console.assert(safePlaceFromHash("#place=abc",[{id:"abc"}])?.id==="abc");console.assert(safePlaceFromHash("#place=missing",[{id:"abc"}])===null);console.log("ERN place routing hardening smoke checks passed");