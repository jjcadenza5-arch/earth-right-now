import { buildAtlas } from "../src/atlas-model.js";const now=new Date().toISOString(),base={permission:"LINK_ONLY",health:"HEALTHY",playback:"EXTERNAL",truth:"EXTERNAL_LIVE",sourceUrl:"https://example.com",checkedAt:now,lastSuccessfulCheck:now,lat:1,lon:1,quality:60,freshness:60,moment:60};const stale={...base,id:"stale",quality:100,checkedAt:"2020-01-01",lastSuccessfulCheck:"2020-01-01"},current={...base,id:"current"};console.assert(buildAtlas([stale,current]).filtered[0].id==="current");console.log("ERN Atlas ordering smoke checks passed");