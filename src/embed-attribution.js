import { safeHttpUrl,externalAttrs } from "./url-safety.js";import { element } from "./safe-dom.js";export function attributionModel(source){const href=safeHttpUrl(source?.sourceUrl||source?.officialUrl);return{provider:source?.provider||"Source",href,required:source?.permission==="EMBED_ALLOWED"||source?.permission==="PARTNER_PERMISSION"}}export function attributionView(source){const m=attributionModel(source),wrap=element("div",{className:"ern-media-credit"});wrap.append(element("span",{text:"Live view: "}));if(m.href){const a=element("a",{text:m.provider});a.href=m.href;Object.assign(a,externalAttrs());wrap.append(a)}else wrap.append(element("span",{text:m.provider}));return wrap}
