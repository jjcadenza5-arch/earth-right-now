import { element } from "./safe-dom.js";import { destinationSummary } from "./destination-engine.js";export function destinationCardView(place,{onOpen}={}){const x=destinationSummary(place),card=element("article",{className:"card destination-card"}),copy=element("div"),meta=element("span",{className:"meta",text:place.country||""}),title=element("h3",{text:place.title}),region=element("p",{text:place.region||""}),count=element("span",{className:"count",text:`${x.windows} window${x.windows===1?"":"s"}${x.hasChoice?" · choose your view":""}`}),actions=element("div",{className:"card-actions"}),open=element("button",{text:"Choose a window"});open.onclick=()=>onOpen?.(place);copy.append(meta,title,region,count);actions.append(open);card.append(copy,actions);return card}
