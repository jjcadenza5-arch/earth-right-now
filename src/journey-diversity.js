export function journeyDiversity(sources=[]){const countries=new Set(),places=new Set(),categories=new Set();for(const s of sources){if(s?.country)countries.add(s.country);if(s?.placeId)places.add(s.placeId);for(const x of s?.categories||[])categories.add(x)}return{windows:sources.length,countries:countries.size,places:places.size,categories:categories.size}}
export function journeyDiversityCopy(sources=[]){const x=journeyDiversity(sources);if(!x.windows)return"No verified-current journey is available right now.";return x.windows+" windows · "+x.places+" places · "+x.countries+" countries"}
