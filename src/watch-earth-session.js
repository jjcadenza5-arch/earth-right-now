import { playbackCapability } from "./playback-capability.js";import { currentSource } from "./discovery-eligibility.js";export function createWatchEarthSession({sources,player,onChange=()=>{},openSource=null}){let index=0,playing=false,timer=null;const list=[...(sources||[])].filter(s=>currentSource(s)&&playbackCapability(s).action!=="UNAVAILABLE");function snapshot(){return{index,total:list.length,source:list[index]||null,playing}}function emit(){onChange(snapshot())}function show(i){if(!list.length)return null;index=(i+list.length)%list.length;const s=list[index];if(openSource)openSource(s,{surface:"watch-earth"});else player.play(s.id,{surface:"watch-earth"});emit();return s}function next(){return show(index+1)}function previous(){return show(index-1)}function pause(){playing=false;if(timer)clearInterval(timer);timer=null;emit()}function resume(ms=18000){if(timer)clearInterval(timer);if(!list.length){playing=false;emit();return}playing=true;timer=setInterval(next,Math.max(8000,Number(ms)||18000));emit()}function stop(){pause();index=0}function destroy(){pause();list.length=0;emit()}return{show,next,previous,pause,resume,stop,destroy,state:snapshot}}
