export function readJSON(storage,key,fallback){try{const raw=storage?.getItem?.(key);if(!raw)return fallback;return JSON.parse(raw)}catch{return fallback}}export function writeJSON(storage,key,value){try{storage?.setItem?.(key,JSON.stringify(value));return true}catch{return false}}export function removeStored(storage,key){try{storage?.removeItem?.(key);return true}catch{return false}}export function stringArray(value,{limit=100}={}){if(!Array.isArray(value))return[];return[...new Set(value.filter(x=>typeof x==="string"&&x.trim()).map(x=>x.trim()))].slice(0,limit)}
