import { allowedEmbedUrl } from "./embed-policy.js";export function playbackCapability(source){if(!source)return{action:"UNAVAILABLE",label:"Unavailable"};if(source.health==="OFFLINE")return{action:"UNAVAILABLE",label:"Temporarily unavailable"};if(source.permission==="LINK_ONLY"||source.playback==="EXTERNAL")return{action:"EXTERNAL",label:"Open current source"};if(source.playback==="EMBED"&&source.embedUrl&&allowedEmbedUrl(source.embedUrl))return{action:"PLAY",label:"Watch live"};if(source.playback==="IMAGE_REFRESH")return{action:"PLAY",label:"View current image"};return source.sourceUrl?{action:"EXTERNAL",label:"Open source"}:{action:"UNAVAILABLE",label:"Unavailable"}}export function canPlayInsideERN(source){return playbackCapability(source).action==="PLAY"}
