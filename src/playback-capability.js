import { allowedEmbedUrl } from "./embed-policy.js";import { safeHttpUrl } from "./url-safety.js";export function playbackCapability(source){if(!source)return{action:"UNAVAILABLE",label:"Unavailable"};if(source.health==="OFFLINE")return{action:"UNAVAILABLE",label:"Temporarily unavailable"};const external=safeHttpUrl(source.sourceUrl||source.officialUrl);if(source.permission==="LINK_ONLY"||source.playback==="EXTERNAL")return external?{action:"EXTERNAL",label:"Open current source"}:{action:"UNAVAILABLE",label:"Unavailable"};if(source.playback==="EMBED"&&source.embedUrl&&allowedEmbedUrl(source.embedUrl))return{action:"PLAY",label:"Watch live"};if(source.playback==="IMAGE_REFRESH"&&safeHttpUrl(source.sourceUrl))return{action:"PLAY",label:"View current image"};return external?{action:"EXTERNAL",label:"Open source"}:{action:"UNAVAILABLE",label:"Unavailable"}}export function canPlayInsideERN(source){return playbackCapability(source).action==="PLAY"}
