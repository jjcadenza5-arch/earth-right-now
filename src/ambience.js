const KEY="ern:ambience";function read(){try{return localStorage.getItem(KEY)==="on"}catch{return false}}function write(on){try{localStorage.setItem(KEY,on?"on":"off")}catch{}}export function ambienceEnabled(){return read()}export function setAmbience(on){const value=!!on;write(value);document.documentElement.dataset.ambience=value?"on":"off";return value}export function toggleAmbience(){return setAmbience(!ambienceEnabled())}export function initAmbience(){const on=ambienceEnabled();document.documentElement.dataset.ambience=on?"on":"off";return on}
