function stopElement(el){const tag=el.tagName;if(tag==="VIDEO"||tag==="AUDIO"){try{el.pause()}catch{}el.removeAttribute("src");for(const s of el.querySelectorAll("source"))s.removeAttribute("src");try{el.load?.()}catch{}}else if(tag==="IFRAME"){try{el.src="about:blank"}catch{}}else if(tag==="IMG"){el.removeAttribute("src")}}export function destroyMedia(mount){if(!mount)return;for(const el of mount.querySelectorAll("iframe,video,audio,img"))stopElement(el);mount.replaceChildren()}export function mediaCount(root=document){return root.querySelectorAll("iframe,video,audio").length}export function activeMedia(root=document){return[...root.querySelectorAll("iframe,video,audio")].filter(el=>el.isConnected)}
