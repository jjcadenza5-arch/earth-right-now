export function destroyMedia(mount){if(!mount)return;for(const el of mount.querySelectorAll("iframe,video,audio,img")){if(el.tagName==="VIDEO"||el.tagName==="AUDIO"){try{el.pause()}catch{}el.removeAttribute("src");el.load?.()}if(el.tagName==="IFRAME")el.src="about:blank"}mount.replaceChildren()}export function mediaCount(root=document){return root.querySelectorAll("iframe,video,audio").length}
