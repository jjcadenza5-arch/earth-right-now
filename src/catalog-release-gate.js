import { guardCatalog } from "./catalog-guard.js";import { catalogHealthSummary } from "./catalog-health-summary.js";export function catalogReleaseGate(rows){const {valid,rejected}=guardCatalog(rows),health=catalogHealthSummary(valid);const duplicateOrMalformed=rejected.length;const currentHealthy=valid.filter(s=>s.health==="HEALTHY"&&["CURRENT_CHECK","STALE_CHECK"].includes((()=>{const t=Date.parse(s.lastSuccessfulCheck||s.checkedAt);if(!Number.isFinite(t))return"UNKNOWN";const hours=Math.max(0,(Date.now()-t)/36e5),max=s.playback==="EXTERNAL"?168:24;return hours<=max?"CURRENT_CHECK":hours<=max*3?"STALE_CHECK":"EXPIRED_CHECK"})())).length;return {ready:duplicateOrMalformed===0&&valid.length>0,valid:valid.length,rejected:rejected.length,currentHealthy,health}}
