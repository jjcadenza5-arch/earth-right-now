export function online(){return navigator.onLine!==false}export function watchNetwork(cb){const fn=()=>cb(online());addEventListener("online",fn);addEventListener("offline",fn);return()=>{removeEventListener("online",fn);removeEventListener("offline",fn)}}export function networkMessage(isOnline){return isOnline?"Connection restored.":"You appear to be offline. Saved Earth information is still available."}
