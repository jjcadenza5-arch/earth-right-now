export function placeHealth(sources){const counts={HEALTHY:0,DEGRADED:0,UNKNOWN:0,OFFLINE:0};for(const s of sources)counts[s.health]=(counts[s.health]||0)+1;const current=counts.HEALTHY>0;return {counts,current,label:current?"Current window available":counts.UNKNOWN?"Sources being checked":counts.DEGRADED?"Limited availability":"No current window"}}export function placeTruthSummary(sources){const types=[...new Set(sources.map(s=>s.truth).filter(Boolean))];return types}
