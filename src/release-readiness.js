import { catalogReleaseGate } from "./catalog-release-gate.js";const evidenceKeys=["browser","mobile","providerPlayback","accessibility","performance","rollback"];function evidence(value){if(value===true)return{ok:true,note:"legacy boolean evidence"};if(!value||typeof value!=="object")return{ok:false,note:null};const note=String(value.note||"").trim(),checkedAt=String(value.checkedAt||"").trim(),validDate=Number.isFinite(Date.parse(checkedAt));return{ok:value.ok===true&&note.length>0&&validDate,note:note||null,checkedAt:validDate?checkedAt:null}}export function releaseReadiness(rows,options={}){const catalog=catalogReleaseGate(rows,options.catalogOptions||{}),raw={browser:options.browser??options.browserTested,mobile:options.mobile??options.mobileTested,providerPlayback:options.providerPlayback??options.providerPlaybackTested,accessibility:options.accessibility??options.accessibilityTested,performance:options.performance??options.performanceTested,rollback:options.rollback??options.rollbackReady},evidenceReport=Object.fromEntries(evidenceKeys.map(k=>[k,evidence(raw[k])])),checks={catalog:catalog.ready,...Object.fromEntries(evidenceKeys.map(k=>[k,evidenceReport[k].ok]))},blockers=Object.entries(checks).filter(([,ok])=>!ok).map(([name])=>name);return{ready:blockers.length===0,checks,blockers,catalog,evidence:evidenceReport}}
