import { element } from "./safe-dom.js";import { sourceBadges } from "./source-badges.js";import { sourceActionMeta } from "./source-action-labels.js";import { currentWindowEyebrow } from "./current-window-label.js";export function liveWindowView(source,{onOpen}={}){const action=sourceActionMeta(source),b=element("button",{className:"live-window-card",attrs:{"aria-label":action.aria}});b.disabled=action.disabled;const top=element("span",{className:"live-window-state",text:currentWindowEyebrow(source)}),title=element("strong",{text:source.title}),meta=element("small",{text:[source.region,source.country].filter(Boolean).join(" · ")}),badges=element("span",{className:"live-window-badges",text:sourceBadges(source).join(" · ")});b.append(top,title,meta,badges);b.onclick=()=>onOpen?.(source);return b}
