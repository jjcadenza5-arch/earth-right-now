import { rankWindows } from "./window-choice.js";import { currentWindowEyebrow } from "./current-window-label.js";export function windowStrip(sources,{limit=8}={}){return rankWindows(sources).slice(0,limit)}export function windowStripLabel(source,index){const place=source.region||source.country||"";return{eyebrow:source.health==="UNKNOWN"?"SOURCE CHECK":currentWindowEyebrow(source),title:source.title,meta:[place,index>0?`View ${index+1}`:null].filter(Boolean).join(" · ")}}
