import { rankWindows } from "./window-choice.js";import { currentWindowEyebrow } from "./current-window-label.js";import { uniqueByVisibleWindow } from "./visible-window-variety.js";
export function windowStrip(sources,{limit=8,now=new Date()}={}){return uniqueByVisibleWindow(rankWindows(sources,{now}),{limit})}
export function windowStripLabel(source,index,{now=new Date()}={}){const place=source.region||source.country||"";return{eyebrow:source.health==="UNKNOWN"?"SOURCE CHECK":currentWindowEyebrow(source,{now}),title:source.title,meta:[place,index>0?`View ${index+1}`:null].filter(Boolean).join(" · ")}}
