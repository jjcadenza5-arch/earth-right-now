import { rankWindows } from "./window-choice.js";export function windowStrip(sources,{limit=8}={}){return rankWindows(sources).slice(0,limit)}export function windowStripLabel(source,index){const place=source.region||source.country||"";return {eyebrow:source.truth==="LIVE_IMAGE"?"CURRENT IMAGE":source.truth==="EXTERNAL_LIVE"?"EXTERNAL LIVE":source.truth==="PARTNER"?"PARTNER":source.health==="HEALTHY"?"LIVE WINDOW":"SOURCE CHECK",title:source.title,meta:[place,index>0?`View ${index+1}`:null].filter(Boolean).join(" · ")}}
