import { sourceStatus } from "./health-policy.js";import { recencyState } from "./source-recency.js";import { playbackCapability } from "./playback-capability.js";export function liveInventory(sources,{now=new Date()}={}){return sources.filter(s=>sourceStatus(s).live&&s.health==="HEALTHY"&&recencyState(s,{now})==="CURRENT_CHECK"&&s.permission!=="UNKNOWN"&&playbackCapability(s,{now}).action!=="UNAVAILABLE")}export function inventoryState(sources,options={}){const live=liveInventory(sources,options);return{ready:live.length>0,count:live.length,message:live.length?"Live windows checked and ready.":"ERN is rechecking its live windows. Explore the broader Atlas meanwhile."}}
