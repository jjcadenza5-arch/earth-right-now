import { sourceStatus } from "./health-policy.js";import { recencyState } from "./source-recency.js";import { playbackCapability } from "./playback-capability.js";export function liveInventory(sources){return sources.filter(s=>sourceStatus(s).live&&s.health==="HEALTHY"&&recencyState(s)==="CURRENT_CHECK"&&s.permission!=="UNKNOWN"&&playbackCapability(s).action!=="UNAVAILABLE")}export function inventoryState(sources){const live=liveInventory(sources);return{ready:live.length>0,count:live.length,message:live.length?"Live windows checked and ready.":"ERN is rechecking its live windows. Explore the broader Atlas meanwhile."}}
