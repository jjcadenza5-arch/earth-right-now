import { discoverableSource } from "./discovery-eligibility.js";import { sourceScore } from "./source-score.js";const norm=x=>(x||"").toString().normalize("NFKD").toLowerCase().replace(/[\u0300-\u036f]/g,"");export function searchEarth(sources,q){const pool=sources.filter(discoverableSource),terms=norm(q).split(/\s+/).filter(Boolean);if(!terms.length)return[...pool].sort((a,b)=>sourceScore(b)-sourceScore(a));return pool.map(s=>{const title=norm(s.title),country=norm(s.country),region=norm(s.region),hay=norm([s.title,s.region,s.country,s.provider,s.story,...(s.categories||[]),...(s.aliases||[])].join(" "));let score=sourceScore(s)*.15;for(const t of terms){if(hay.includes(t))score+=10;if(title.includes(t))score+=18;if(country.includes(t)||region.includes(t))score+=9}return{s,score}}).filter(x=>x.score>0).sort((a,b)=>b.score-a.score).map(x=>x.s)}
