import { discoverableSource,currentSource } from "./discovery-eligibility.js";
import { sourceScore } from "./source-score.js";
import { interpretEarthIntent } from "./earth-intent.js";
import { solarMoment } from "./solar-moment.js";
const norm=x=>(x||"").toString().normalize("NFKD").toLowerCase().replace(/[\u0300-\u036f]/g,"").replace(/[^\p{L}\p{N}\s-]/gu," ").replace(/\s+/g," ").trim();
const currentWords=new Set(["now","live","current","today","tonight"]);
const stopWords=new Set(["show","me","find","see","watch","look","at","in","on","the","a","an","of","for","please","i","want","to","go","going","visit","before","what","is","like","there","can","you","right"]);
export function searchEarth(sources,q,{now=new Date()}={}){
 const terms=norm(q).split(/\s+/).filter(Boolean),intent=interpretEarthIntent(q),wantsCurrent=terms.some(t=>currentWords.has(t)),intentWords=new Set(intent.intents.flatMap(i=>({wildlife:["wildlife","animal","animals","elephant","bird","birds","zoo","nature"],water:["beach","beaches","sea","ocean","coast","coastal","water","surf","harbour","harbor"],mountains:["mountain","mountains","volcano","volcanoes","snow","summit","alpine"],human:["city","cities","street","streets","people","urban","square","harbour","harbor"],beautiful:["beautiful","scenic","view","views","relax","relaxing","calm","peaceful"],happening:["happening","active","event","busy","crowd","crowds"],night:["night","nighttime","lights","after","dark"],daylight:["daylight","sunny","daytime"],golden:["sunrise","sunset","golden","hour","dawn","dusk"],reference:["photo","photos","image","images","reference","picture","pictures"]}[i]||[]))),semanticTerms=terms.filter(t=>!currentWords.has(t)&&!stopWords.has(t)&&!intentWords.has(t)),pool=sources.filter(s=>wantsCurrent?currentSource(s,{now}):discoverableSource(s));
 if(!terms.length)return[...pool].sort((a,b)=>sourceScore(b,{now})-sourceScore(a,{now}));
 const intentMatch=s=>{const cats=norm([(s.categories||[]).join(" "),s.title,s.story].join(" ")),phase=solarMoment(s,now).phase;return intent.intents.every(i=>i==="wildlife"?/wildlife|animal|zoo|nature/.test(cats):i==="water"?/beach|sea|ocean|coast|water|surf|harbour|harbor/.test(cats):i==="mountains"?/mountain|volcano|snow|summit|alpine|ski/.test(cats):i==="human"?/city|street|urban|square|harbour|harbor|landmark/.test(cats):i==="night"?phase==="NIGHT":i==="daylight"?phase==="DAY":i==="golden"?["SUNRISE","SUNSET","MORNING_GOLDEN","EVENING_GOLDEN"].includes(phase):i==="reference"?s.truth==="PREVIEW"||s.playback==="PREVIEW":true)};
 const intentPool=intent.intents.length?pool.filter(intentMatch):pool;
 if(!semanticTerms.length)return[...intentPool].sort((a,b)=>sourceScore(b,{now})-sourceScore(a,{now}));
 return intentPool.map(s=>{const title=norm(s.title),country=norm(s.country),region=norm(s.region),hay=norm([s.title,s.region,s.country,s.provider,s.story,...(s.categories||[]),...(s.aliases||[])].join(" "));let score=sourceScore(s,{now})*.15,hits=0;for(const t of semanticTerms){let hit=false;if(hay.includes(t)){score+=10;hit=true}if(title.includes(t)){score+=18;hit=true}if(country.includes(t)||region.includes(t)){score+=9;hit=true}if(hit)hits++}return{s,score,hits}}).filter(x=>x.hits>0).sort((a,b)=>b.hits-a.hits||b.score-a.score).map(x=>x.s)
}
