const norm=x=>(x||"").toString().normalize("NFKD").toLowerCase().replace(/[\u0300-\u036f]/g,"");export function searchEarth(sources,q){const terms=norm(q).split(/\s+/).filter(Boolean);if(!terms.length)return sources;return sources.map(s=>{const hay=norm([s.title,s.region,s.country,s.provider,s.story,...(s.categories||[]),...(s.aliases||[])].join(" "));let score=0;for(const t of terms){if(hay.includes(t))score+=10;if(norm(s.title).includes(t))score+=15;if(norm(s.country).includes(t)||norm(s.region).includes(t))score+=8}return {s,score}}).filter(x=>x.score>0).sort((a,b)=>b.score-a.score).map(x=>x.s)}
