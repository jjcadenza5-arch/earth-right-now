import { discoverableSource,currentSource } from "./discovery-eligibility.js";import { sourceScore } from "./source-score.js";const norm=x=>(x||"").toString().normalize("NFKD").toLowerCase().replace(/[\u0300-\u036f]/g,"").replace(/[^\p{L}\p{N}\s-]/gu," ").replace(/\s+/g," ").trim();const currentWords=new Set(["now","live","current","today"]);export function searchEarth(sources,q){const terms=norm(q).split(/\s+/).filter(Boolean),wantsCurrent=terms.some(t=>currentWords.has(t)),semanticTerms=terms.filter(t=>!currentWords.has(t)),pool=sources.filter(s=>wantsCurrent?currentSource(s):discoverableSource(s));if(!terms.length)return[...pool].sort((a,b)=>sourceScore(b)-sourceScore(a));if(wantsCurrent&&!semanticTerms.length)return[...pool].sort((a,b)=>sourceScore(b)-sourceScore(a));return pool.map(s=>{const title=norm(s.title),country=norm(s.country),region=norm(s.region),hay=norm([s.title,s.region,s.country,s.provider,s.story,...(s.categories||[]),...(s.aliases||[])].join(" "));let score=sourceScore(s)*.15,hits=0;for(const t of semanticTerms){let hit=false;if(hay.includes(t)){score+=10;hit=true}if(title.includes(t)){score+=18;hit=true}if(country.includes(t)||region.includes(t)){score+=9;hit=true}if(hit)hits++}return{s,score,hits}}).filter(x=>x.hits>0).sort((a,b)=>b.hits-a.hits||b.score-a.score).map(x=>x.s)}
