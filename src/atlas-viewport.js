import { projectEquirectangular } from "./map-projection.js";export function visibleSources(sources,{width=100,height=50,bounds=null}={}){return sources.map(source=>({source,point:projectEquirectangular(source.lat,source.lon,width,height)})).filter(x=>x.point&&(!bounds||(x.point.x>=bounds.left&&x.point.x<=bounds.right&&x.point.y>=bounds.top&&x.point.y<=bounds.bottom)))}export function pinLabel(source){return [source.title,source.country].filter(Boolean).join(", ")}
