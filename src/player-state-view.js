export function playerStateView(state,source){const history=state.history||[],cursor=Number.isInteger(state.cursor)?state.cursor:-1;return{open:!!state.open,title:source?.title||"",surface:state.surface||"",mode:state.mode||"",canPrevious:cursor>0,canNext:cursor>=0&&cursor<history.length-1,external:state.mode==="EXTERNAL",unavailable:state.mode==="UNAVAILABLE"}}export function viewerAnnouncement(view){if(!view.open)return"Viewer closed";if(view.external)return`${view.title}. Opens at source provider.`;if(view.unavailable)return`${view.title}. Source unavailable.`;return`${view.title}. Viewer open.`}
