export function playerStateView(state,source){return {open:!!state.open,title:source?.title||"",surface:state.surface||"",mode:state.mode||"",canPrevious:(state.history?.length||0)>1,canNext:(state.history?.length||0)>1,external:state.mode==="EXTERNAL",unavailable:state.mode==="UNAVAILABLE"}}export function viewerAnnouncement(view){if(!view.open)return "Viewer closed";if(view.external)return `${view.title}. Opens at source provider.`;if(view.unavailable)return `${view.title}. Source unavailable.`;return `${view.title}. Viewer open.`}
