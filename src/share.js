import { placeShareUrl,placeShareText } from "./place-share.js";export async function sharePlace({title,placeId},{navigatorRef=typeof navigator!=="undefined"?navigator:null}={}){if(!placeId||!navigatorRef)return false;const url=placeShareUrl(placeId);if(!url)return false;const data={title:(title||"Earth Right Now")+" — Earth Right Now",text:"See before you go.",url};if(typeof navigatorRef.share==="function"){try{await navigatorRef.share(data);return true}catch(e){if(e?.name==="AbortError")return false}}try{await navigatorRef.clipboard?.writeText(url);return true}catch{return false}}export function shareData(place){if(!place?.id)return null;return{title:`${place.title||"Earth Right Now"} — Earth Right Now`,text:placeShareText(place),url:placeShareUrl(place.id)}}
