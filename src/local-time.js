export function localTime(timeZone,now=new Date()){try{return new Intl.DateTimeFormat(undefined,{timeZone,hour:"2-digit",minute:"2-digit",hourCycle:"h23"}).format(now)}catch{return null}}export function localHour(timeZone,now=new Date()){try{const parts=new Intl.DateTimeFormat("en-US",{timeZone,hour:"numeric",hourCycle:"h23"}).formatToParts(now);const h=Number(parts.find(p=>p.type==="hour")?.value);return Number.isFinite(h)?h:null}catch{return null}}export function daylightBand(timeZone,now=new Date()){const h=localHour(timeZone,now);if(h==null)return"unknown";if(h>=6&&h<10)return"morning";if(h>=10&&h<17)return"daylight";if(h>=17&&h<20)return"evening-light";return"night"}export function likelyDaylight(timeZone,now=new Date()){const h=localHour(timeZone,now);return h==null?null:h>=6&&h<20}
