export function atlasFilter(sources,{liveOnly=false,daylightOnly=false,family=null,health=null}={}){return sources.filter(s=>{if(liveOnly&&!["LIVE_VIDEO","LIVE_IMAGE","EXTERNAL_LIVE","PARTNER"].includes(s.truth))return false;if(family&&!s.categories?.includes(family))return false;if(health&&s.health!==health)return false;if(daylightOnly&&s._daylight===false)return false;return true})}
