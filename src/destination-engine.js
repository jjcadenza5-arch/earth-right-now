import { playbackCapability } from "./playback-capability.js";import { currentSource,discoverableSource } from "./discovery-eligibility.js";import { sourceScore } from "./source-score.js";export function destinationSummary(place){const windows=place?.sources||[],actionable=windows.filter(discoverableSource),healthy=actionable.filter(s=>s.health==="HEALTHY").length,current=actionable.filter(currentSource).length;return{windows:actionable.length,totalWindows:windows.length,healthy,current,hasChoice:actionable.length>1}}export function destinationRank(place){const sources=(place?.sources||[]).filter(discoverableSource);if(!sources.length)return-Infinity;const current=sources.filter(currentSource),pool=current.length?current:sources;return Math.max(...pool.map(s=>sourceScore(s)+(currentSource(s)?20:0)+(playbackCapability(s).action==="PLAY"?8:0)))}export function rankedDestinations(places){return[...places].filter(p=>destinationSummary(p).windows>0).sort((a,b)=>destinationRank(b)-destinationRank(a))}
