import { playbackCapability } from "./playback-capability.js";import { recencyState } from "./source-recency.js";export function destinationSummary(place){const windows=place.sources||[],actionable=windows.filter(s=>playbackCapability(s).action!=="UNAVAILABLE"),healthy=actionable.filter(s=>s.health==="HEALTHY").length,current=actionable.filter(s=>s.health==="HEALTHY"&&recencyState(s)==="CURRENT_CHECK"&&["LIVE_VIDEO","LIVE_IMAGE","EXTERNAL_LIVE","PARTNER"].includes(s.truth)).length;return{windows:actionable.length,totalWindows:windows.length,healthy,current,hasChoice:actionable.length>1}}export function destinationRank(place){const scores=(place.sources||[]).filter(s=>playbackCapability(s).action!=="UNAVAILABLE").map(s=>(s.quality||0)*.5+(s.moment||0)*.35+(s.freshness||0)*.15+(playbackCapability(s).action==="PLAY"?12:0));return scores.length?Math.max(...scores):0}export function rankedDestinations(places){return[...places].sort((a,b)=>destinationRank(b)-destinationRank(a))}
