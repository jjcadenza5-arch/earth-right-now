import { recencyState } from "./source-recency.js";import { playbackCapability } from "./playback-capability.js";export function sourceScore(s){let score=(s.quality||0)*.5+(s.freshness||0)*.25+(s.moment||0)*.15;if(s.health==="HEALTHY")score+=12;else if(s.health==="DEGRADED")score-=8;else if(s.health==="UNKNOWN")score-=18;else if(s.health==="OFFLINE")score-=100;if(playbackCapability(s).action==="PLAY")score+=8;if(recencyState(s)==="CURRENT_CHECK")score+=8;else if(recencyState(s)==="EXPIRED_CHECK")score-=12;return score}
