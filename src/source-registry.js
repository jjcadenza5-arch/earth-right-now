import { PlaybackController } from "./playback-controller.js";import { registryFromGuard } from "./catalog-guard.js";import { sourceScore } from "./source-score.js";export async function loadRegistryReport(url="./data/sources.json"){const response=await fetch(url,{cache:"no-store"});if(!response.ok)throw new Error(`ERN source registry failed: ${response.status}`);const rows=await response.json();if(!Array.isArray(rows))throw new Error("ERN source registry must be an array");return registryFromGuard(rows)}export async function loadRegistry(url="./data/sources.json"){const{registry,rejected}=await loadRegistryReport(url);if(rejected.length)console.warn("ERN rejected malformed source records",rejected.map(x=>({id:x.row?.id,errors:x.errors})));return registry}export function rankedSources(registry,predicate=()=>true){return[...registry.values()].filter(predicate).sort((a,b)=>sourceScore(b)-sourceScore(a))}export function createERNPlayback(registry,onStateChange){return new PlaybackController({registry,onStateChange})}
