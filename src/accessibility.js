export function trapViewer(viewer){const focusable=()=>[...viewer.querySelectorAll('button:not([hidden]),a[href]:not([hidden]),input:not([hidden]),select:not([hidden]),textarea:not([hidden]),[tabindex]:not([tabindex="-1"]):not([hidden])')].filter(x=>!x.disabled&&x.getAttribute("aria-hidden")!=="true");function onKey(e){if(e.key!=="Tab")return;const list=focusable();if(!list.length){e.preventDefault();viewer.focus?.({preventScroll:true});return}const first=list[0],last=list[list.length-1];if(!list.includes(document.activeElement)){e.preventDefault();(e.shiftKey?last:first).focus({preventScroll:true})}else if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus({preventScroll:true})}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus({preventScroll:true})}}viewer.addEventListener("keydown",onKey);return()=>viewer.removeEventListener("keydown",onKey)}export function reducedMotion(){try{return matchMedia("(prefers-reduced-motion: reduce)").matches}catch{return false}}
