export function trapViewer(viewer){const focusable=()=>[...viewer.querySelectorAll('button,a[href],input,[tabindex]:not([tabindex="-1"])')].filter(x=>!x.disabled);function onKey(e){if(e.key!=="Tab")return;const list=focusable();if(!list.length)return;const first=list[0],last=list[list.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}viewer.addEventListener("keydown",onKey);return()=>viewer.removeEventListener("keydown",onKey)}
export function reducedMotion(){return matchMedia("(prefers-reduced-motion: reduce)").matches}
