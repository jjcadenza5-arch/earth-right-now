export function placeHref(placeId){return"#place="+encodeURIComponent(placeId)}export function parsePlaceHash(hash=location.hash){const m=hash.match(/^#place=(.+)$/);if(!m)return null;try{return decodeURIComponent(m[1])}catch{return null}}export function sourcesForPlace(registry,placeId){return[...registry.values()].filter(s=>(s.placeId||s.id)===placeId)}export function safePlaceFromHash(hash,places){const id=parsePlaceHash(hash);return id?places.find(p=>p.id===id)||null:null}
