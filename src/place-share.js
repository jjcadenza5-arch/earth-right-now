import { placeHref } from "./place-routing.js";export function placeShareUrl(placeId,{origin=typeof location!=="undefined"?location.origin:"",pathname=typeof location!=="undefined"?location.pathname:"/"}={}){if(!placeId)return null;const base=(origin||"")+(pathname||"/");return base+placeHref(placeId)}export function placeShareText(place){return place?.title?`${place.title} — explore this place and its current-source status on Earth Right Now`:"Explore this place and its current-source status on Earth Right Now"}
