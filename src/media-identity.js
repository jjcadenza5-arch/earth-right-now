function youtubeId(raw){try{const u=new URL(raw),h=u.hostname.toLowerCase().replace(/^www\./,"");if(h==="youtu.be")return u.pathname.split("/").filter(Boolean)[0]||null;if(h.endsWith("youtube.com")){if(u.pathname==="/watch")return u.searchParams.get("v");const m=u.pathname.match(/^\/(?:embed|live|shorts)\/([^/?#]+)/);return m?.[1]||null}}catch{}return null}
function normalizedUrl(raw){try{const u=new URL(raw);u.hash="";for(const k of ["cb","cache","cachebust","cachebuster","_","t"])u.searchParams.delete(k);u.searchParams.sort();return u.toString()}catch{return String(raw||"")}}
export function mediaIdentity(s){if(!s)return"";for(const raw of [s.embedUrl,s.imageUrl,s.sourceUrl,s.officialUrl]){if(!raw)continue;const yt=youtubeId(raw);if(yt)return"youtube:"+yt;if(raw===s.officialUrl&&s.embedUrl)return normalizedUrl(s.embedUrl);return normalizedUrl(raw)}return s.id?"id:"+s.id:""}
