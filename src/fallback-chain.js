import { playbackCapability } from "./playback-capability.js";import { allowedEmbedUrl } from "./embed-policy.js";import { safeHttpUrl } from "./url-safety.js";export function fallbackChain(source){const chain=[],cap=playbackCapability(source);if(cap.action==="PLAY"&&source.playback==="EMBED"){const url=allowedEmbedUrl(source.embedUrl);if(url)chain.push({mode:"EMBED",url})}if(cap.action==="PLAY"&&source.playback==="IMAGE_REFRESH"){const url=safeHttpUrl(source.sourceUrl);if(url)chain.push({mode:"IMAGE_REFRESH",url})}const external=safeHttpUrl(source.sourceUrl||source.officialUrl);if(external&&cap.action!=="UNAVAILABLE")chain.push({mode:"EXTERNAL",url:external});chain.push({mode:"UNAVAILABLE",url:null});return chain}export function nextFallback(chain,currentMode){const i=chain.findIndex(x=>x.mode===currentMode);if(i<0)return chain[0]||{mode:"UNAVAILABLE",url:null};return chain[Math.min(i+1,chain.length-1)]||chain.at(-1)}
