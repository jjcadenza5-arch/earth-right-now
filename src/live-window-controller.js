import { liveInventory,inventoryState } from "./live-inventory.js";export function createLiveWindowController(sources){const live=liveInventory(sources);let index=0;return {state:()=>inventoryState(sources),all:()=>[...live],current:()=>live[index]||null,next(){if(!live.length)return null;index=(index+1)%live.length;return live[index]},previous(){if(!live.length)return null;index=(index-1+live.length)%live.length;return live[index]}}}
