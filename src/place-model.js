export function groupByPlace(sources){const map=new Map();for(const s of sources){if(s.id==="recovery-placeholder")continue;const key=s.placeId||s.id;if(!map.has(key))map.set(key,{id:key,title:s.title,country:s.country,region:s.region,lat:s.lat,lon:s.lon,categories:new Set(),sources:[]});const p=map.get(key);p.sources.push(s);for(const c of s.categories||[])p.categories.add(c)}return [...map.values()].map(p=>({...p,categories:[...p.categories]}))}
export function bestWindow(place){return [...place.sources].sort((a,b)=>((b.quality||0)+(b.moment||0))-((a.quality||0)+(a.moment||0)))[0]}
