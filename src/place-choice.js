import { rankWindows } from "./window-choice.js";import { placeHealth } from "./place-health.js";export function placeChoice(place,sources,{now=new Date()}={}){const windows=rankWindows(sources,{now});return {place,windows,health:placeHealth(windows,{now}),preferred:windows[0]||null,alternates:windows.slice(1)}}