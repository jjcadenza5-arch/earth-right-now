import { requiresRecheck } from "./source-recency.js";import { needsDeepValidation } from "./provider-policy.js";export function buildRevalidationQueue(sources){return sources.filter(s=>s.health==="UNKNOWN"||s.health==="DEGRADED"||requiresRecheck(s)).map(s=>({source:s,priority:(s.health==="UNKNOWN"?40:0)+(needsDeepValidation(s)?30:0)+(s.playback==="EMBED"?20:0)+(s.quality||0)/10})).sort((a,b)=>b.priority-a.priority)}
