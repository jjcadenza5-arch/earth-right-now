import { playbackCapability } from "./playback-capability.js";export function discoverableSource(source){return !!source&&source.id!=="recovery-placeholder"&&source.health!=="OFFLINE"&&playbackCapability(source).action!=="UNAVAILABLE"}export function currentSource(source){return discoverableSource(source)&&source.health==="HEALTHY"&&source.permission!=="UNKNOWN"}export function discoveryPool(sources){return sources.filter(discoverableSource)}
