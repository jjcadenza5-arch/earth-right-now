import { safeHttpUrl } from "./url-safety.js";export function posterFor(source){const url=safeHttpUrl(source?.thumbnailUrl);if(url)return {kind:"image",url,alt:source?.title||"Earth Right Now"};return {kind:"generated",url:null,alt:source?.title||"Earth Right Now"}}export function posterClass(source){const cats=(source?.categories||[]).join(" ").toLowerCase();if(/sea|coast|harbour|beach|water/.test(cats))return "poster-water";if(/wildlife|animal/.test(cats))return "poster-wildlife";if(/mountain|volcano/.test(cats))return "poster-mountain";if(/city|urban/.test(cats))return "poster-city";return "poster-earth"}export function heroPosterStyle(source){const poster=posterFor(source);return poster.kind==="image"?{className:"has-poster",backgroundImage:`url("${poster.url.replaceAll('"',"%22")}")`}:{className:posterClass(source),backgroundImage:""}}
