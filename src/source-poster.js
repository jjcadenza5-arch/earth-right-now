export function posterFor(source){if(source.thumbnailUrl)return {kind:"image",url:source.thumbnailUrl,alt:source.title};return {kind:"generated",url:null,alt:source.title}}export function posterClass(source){const cats=(source.categories||[]).join(" ").toLowerCase();if(/sea|coast|harbour|beach|water/.test(cats))return "poster-water";if(/wildlife|animal/.test(cats))return "poster-wildlife";if(/mountain|volcano/.test(cats))return "poster-mountain";if(/city|urban/.test(cats))return "poster-city";return "poster-earth"}
