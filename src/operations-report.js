import { catalogHealthSummary } from "./catalog-health-summary.js";import { buildRevalidationQueue } from "./revalidation-queue.js";import { catalogReleaseGate } from "./catalog-release-gate.js";export function operationsReport(sources,{queueLimit=20,catalogOptions={}}={}){const health=catalogHealthSummary(sources),queue=buildRevalidationQueue(sources),gate=catalogReleaseGate(sources,catalogOptions);return{generatedAt:new Date().toISOString(),health,gate:{ready:gate.ready,blockers:gate.blockers,currentHealthy:gate.currentHealthy,currentInsideERN:gate.currentInsideERN,unknown:gate.unknown,rejected:gate.rejected},revalidation:{total:queue.length,next:queue.slice(0,queueLimit).map(x=>({id:x.source.id,title:x.source.title,priority:x.priority,reason:x.reason,health:x.source.health,playback:x.source.playback}))}}}