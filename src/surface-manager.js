const SURFACES=["explore","history","atlas","moments","submit"];export function createSurfaceManager(){let active=null;function open(id){if(!SURFACES.includes(id))return false;for(const name of SURFACES){const el=document.getElementById(name);if(el)el.dataset.surfaceOpen=String(name===id)}active=id;document.documentElement.dataset.activeSurface=id;return true}function close(){for(const name of SURFACES){const el=document.getElementById(name);if(el)delete el.dataset.surfaceOpen}active=null;delete document.documentElement.dataset.activeSurface}function current(){return active}return {open,close,current}}export {SURFACES};
