export function uniqueByVisibleWindow(sources,{limit=Infinity,onePerPlace=false}={}){const out=[],media=new Set(),places=new Set();for(const s of sources||[]){const key=s.embedUrl||s.imageUrl||s.thumbnailUrl||s.sourceUrl||s.officialUrl||s.id,place=s.placeId||s.id;if(media.has(key)||(onePerPlace&&places.has(place)))continue;media.add(key);places.add(place);out.push(s);if(out.length>=limit)break}return out}
