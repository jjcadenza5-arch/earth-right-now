export function event(name,data={}){if(!globalThis.ERN_TELEMETRY)return;try{globalThis.ERN_TELEMETRY({name,data,at:new Date().toISOString()})}catch{}}export const events={windowOpened:s=>event("window_opened",{sourceId:s.id,placeId:s.placeId,truth:s.truth,playback:s.playback}),favorite:id=>event("favorite_toggled",{sourceId:id}),search:q=>event("earth_search",{length:q.length}),external:s=>event("external_source_opened",{sourceId:s.id,provider:s.provider})};
