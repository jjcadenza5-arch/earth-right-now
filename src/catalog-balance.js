import { catalogCoverage } from "./catalog-coverage.js";export function catalogBalance(sources){const c=catalogCoverage(sources),insideRatio=c.sources?c.playback.insideERN/c.sources:0;const categoryCounts=Object.values(c.categories);return{...c,insideRatio,categorySpread:categoryCounts.length?Math.max(...categoryCounts)-Math.min(...categoryCounts):0,needsInsidePlayback:insideRatio<.35,needsGeographicBreadth:c.countries<12}}export function recoveryNeeds(sources){const b=catalogBalance(sources),needs=[];if(b.needsInsidePlayback)needs.push("inside-ERN playable sources");if(b.needsGeographicBreadth)needs.push("more countries");const sorted=Object.entries(b.categories).sort((a,b)=>a[1]-b[1]);if(sorted.length)needs.push("underrepresented categories: "+sorted.slice(0,3).map(x=>x[0]).join(", "));return needs}
