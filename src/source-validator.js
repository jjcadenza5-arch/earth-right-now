const required=["id","placeId","title","truth","permission","health","playback","sourceUrl"];const enums={truth:new Set(["LIVE_VIDEO","LIVE_IMAGE","EXTERNAL_LIVE","PARTNER","PREVIEW"]),permission:new Set(["EMBED_ALLOWED","LINK_ONLY","PARTNER_PERMISSION","UNKNOWN"]),health:new Set(["HEALTHY","DEGRADED","OFFLINE","UNKNOWN"]),playback:new Set(["EMBED","IMAGE_REFRESH","EXTERNAL","PREVIEW"])};
export function validateSource(s){const errors=[];for(const k of required)if(s[k]===undefined||s[k]===null||s[k]==="")errors.push("missing "+k);for(const [k,set] of Object.entries(enums))if(s[k]&&!set.has(s[k]))errors.push("invalid "+k);if(s.playback==="EMBED"&&s.permission==="EMBED_ALLOWED"&&!s.embedUrl)errors.push("embed playback requires embedUrl");if(s.truth==="PREVIEW"&&s.health==="HEALTHY")errors.push("preview cannot be promoted by health");return errors}
export function auditRegistry(registry){return [...registry.values()].map(source=>({source,errors:validateSource(source)})).filter(x=>x.errors.length)}
