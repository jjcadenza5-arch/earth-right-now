import { sourceStatus } from "./health-policy.js";import { truthDetail } from "./truth-detail.js";import { daylightLabel } from "./daylight-selection.js";import { currentSource } from "./discovery-eligibility.js";export function sourceBadges(s,{includeDaylight=false}={}){const out=[sourceStatus(s).label];if(currentSource(s)&&s.truth==="LIVE_IMAGE")out.push("REFRESHED VIEW");if(s.permission==="LINK_ONLY")out.push("OPENS AT SOURCE");if(s.permission==="PARTNER_PERMISSION")out.push("PARTNER");if(s.health==="DEGRADED")out.push("DEGRADED");if(s.health==="UNKNOWN")out.push("VERIFYING");if(includeDaylight&&s.timeZone)out.push(daylightLabel(s));return[...new Set(out)]}export function sourceBadgeTitle(s){return truthDetail(s)}
