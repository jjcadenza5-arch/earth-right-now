import { sourceStatus } from "./health-policy.js";import { truthDetail } from "./truth-detail.js";import { daylightLabel } from "./daylight-selection.js";import { currentSource } from "./discovery-eligibility.js";
export function sourceBadges(s,{includeDaylight=false,now=new Date()}={}){const out=[sourceStatus(s,{now}).label];if(currentSource(s,{now})&&s.truth==="LIVE_IMAGE")out.push("REFRESHED IMAGE · NEAR NOW");if(s.truth==="PREVIEW"||s.playback==="PREVIEW")out.push("REFERENCE · NOT CURRENT");if(s.permission==="LINK_ONLY")out.push("OPENS AT SOURCE");if(s.permission==="PARTNER_PERMISSION")out.push("PARTNER");if(s.health==="DEGRADED")out.push("DEGRADED");if(s.health==="UNKNOWN")out.push("VERIFYING");if(includeDaylight&&s.timeZone)out.push(daylightLabel(s,now));return[...new Set(out)]}
export function sourceBadgeTitle(s){return truthDetail(s)}
