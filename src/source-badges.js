import { sourceStatus } from "./health-policy.js";import { truthDetail } from "./truth-detail.js";export function sourceBadges(s){const out=[sourceStatus(s).label];if(s.truth==="LIVE_IMAGE")out.push("REFRESHED VIEW");if(s.permission==="LINK_ONLY")out.push("OPENS AT SOURCE");if(s.permission==="PARTNER_PERMISSION")out.push("PARTNER");if(s.health==="DEGRADED")out.push("DEGRADED");if(s.health==="UNKNOWN")out.push("VERIFYING");return [...new Set(out)]}export function sourceBadgeTitle(s){return truthDetail(s)}
