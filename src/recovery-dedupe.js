function norm(url){try{const u=new URL(url);u.hash="";u.search="";return u.href.replace(/\/$/,"")}catch{return String(url||"").trim().replace(/\/$/,"")}}export function recoveryKey(row){return [row.placeId||"",norm(row.sourceUrl),norm(row.officialUrl)].join("|")}export function dedupeRecovery(rows,publicRows=[]){const publicUrls=new Set(publicRows.flatMap(r=>[norm(r.sourceUrl),norm(r.officialUrl)]).filter(Boolean)),seen=new Set(),accepted=[],duplicates=[];for(const row of rows){const key=recoveryKey(row),urls=[norm(row.sourceUrl),norm(row.officialUrl)].filter(Boolean);if(seen.has(key)||urls.some(u=>publicUrls.has(u))){duplicates.push(row);continue}seen.add(key);accepted.push(row)}return {accepted,duplicates}}
