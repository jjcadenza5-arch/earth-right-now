export function normalizeRecoveryCandidate(row){const preserveLinkOnly=row?.permission==="LINK_ONLY";return {...row,permission:preserveLinkOnly?"LINK_ONLY":"UNKNOWN",health:"UNKNOWN",playback:"EXTERNAL",embedUrl:null,checkedAt:null,lastSuccessfulCheck:null,failureReason:"Recovered historical source awaiting current revalidation.",quality:0,freshness:0,moment:0}}export function candidateEligibleForPublicRegistry(row){return row?.health==="HEALTHY"&&row?.permission!=="UNKNOWN"}
