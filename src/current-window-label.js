import { playbackCapability } from "./playback-capability.js";export function currentWindowEyebrow(source){if(!source)return"SOURCE";if(source.truth==="LIVE_IMAGE")return"CURRENT IMAGE";if(source.truth==="PREVIEW")return"PREVIEW";const cap=playbackCapability(source);if(cap.action==="EXTERNAL")return"CURRENT AT SOURCE";if(source.truth==="PARTNER")return"PARTNER LIVE";return"LIVE WINDOW"}export function currentWindowAction(source){const cap=playbackCapability(source);if(cap.action==="EXTERNAL")return"Open current source";if(source.truth==="LIVE_IMAGE")return"View current image";if(cap.action==="PLAY")return"Watch live";return"Unavailable"}
