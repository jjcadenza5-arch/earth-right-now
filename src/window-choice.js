import { sourceStatus } from "./health-policy.js";export function rankWindows(sources){return [...sources].sort((a,b)=>{const ah=a.health==="HEALTHY"?1:0,bh=b.health==="HEALTHY"?1:0;if(ah!==bh)return bh-ah;const ae=a.playback==="EMBED"?1:0,be=b.playback==="EMBED"?1:0;if(ae!==be)return be-ae;return ((b.quality||0)+(b.moment||0))-((a.quality||0)+(a.moment||0))})}export function windowLabel(s){const st=sourceStatus(s);return [st.label,s.provider].filter(Boolean).join(" · ")}
