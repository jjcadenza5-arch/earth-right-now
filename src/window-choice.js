import { sourceStatus } from "./health-policy.js";import { discoverableSource,currentSource } from "./discovery-eligibility.js";import { playbackCapability } from "./playback-capability.js";import { sourceScore } from "./source-score.js";export function rankWindows(sources,{now=new Date()}={}){return(sources||[]).filter(discoverableSource).sort((a,b)=>{const ac=currentSource(a,{now})?1:0,bc=currentSource(b,{now})?1:0;if(ac!==bc)return bc-ac;const ap=playbackCapability(a,{now}).action==="PLAY"?1:0,bp=playbackCapability(b,{now}).action==="PLAY"?1:0;if(ap!==bp)return bp-ap;return sourceScore(b,{now})-sourceScore(a,{now})})}export function windowLabel(s,{now=new Date()}={}){const st=sourceStatus(s,{now});return[st.label,s.provider].filter(Boolean).join(" · ")}
