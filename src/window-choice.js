import { sourceStatus } from "./health-policy.js";import { discoverableSource,currentSource } from "./discovery-eligibility.js";import { playbackCapability } from "./playback-capability.js";import { sourceScore } from "./source-score.js";export function rankWindows(sources){return(sources||[]).filter(discoverableSource).sort((a,b)=>{const ac=currentSource(a)?1:0,bc=currentSource(b)?1:0;if(ac!==bc)return bc-ac;const ap=playbackCapability(a).action==="PLAY"?1:0,bp=playbackCapability(b).action==="PLAY"?1:0;if(ap!==bp)return bp-ap;return sourceScore(b)-sourceScore(a)})}export function windowLabel(s){const st=sourceStatus(s);return[st.label,s.provider].filter(Boolean).join(" · ")}
