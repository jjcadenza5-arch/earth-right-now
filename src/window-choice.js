import { sourceStatus } from "./health-policy.js";import { discoverableSource } from "./discovery-eligibility.js";import { playbackCapability } from "./playback-capability.js";export function rankWindows(sources){return sources.filter(discoverableSource).sort((a,b)=>{const ah=a.health==="HEALTHY"?1:0,bh=b.health==="HEALTHY"?1:0;if(ah!==bh)return bh-ah;const ae=playbackCapability(a).action==="PLAY"?1:0,be=playbackCapability(b).action==="PLAY"?1:0;if(ae!==be)return be-ae;return((b.quality||0)+(b.moment||0))-((a.quality||0)+(a.moment||0))})}export function windowLabel(s){const st=sourceStatus(s);return[st.label,s.provider].filter(Boolean).join(" · ")}
