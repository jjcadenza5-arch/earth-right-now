import { recencyState } from "./source-recency.js";import { playbackCapability } from "./playback-capability.js";export function catalogHealthSummary(sources){const out={total:sources.length,healthy:0,degraded:0,offline:0,unknown:0,current:0,stale:0,expired:0,insideERN:0,external:0,unavailable:0};for(const s of sources){const h=(s.health||"UNKNOWN").toLowerCase();if(h in out)out[h]++;const r=recencyState(s);if(r==="CURRENT_CHECK")out.current++;else if(r==="STALE_CHECK")out.stale++;else if(r==="EXPIRED_CHECK")out.expired++;const a=playbackCapability(s).action;if(a==="PLAY")out.insideERN++;else if(a==="EXTERNAL")out.external++;else out.unavailable++}return out}
