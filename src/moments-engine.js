import { discoverableSource,currentSource } from "./discovery-eligibility.js";import { sourceScore } from "./source-score.js";const editorial=["Beautiful Earth","Interesting Earth","Useful Earth","Earth Happening Now"];export function momentFamilies(s){return(s.categories||[]).filter(c=>editorial.includes(c))}export function momentEligible(s){return discoverableSource(s)&&momentFamilies(s).length>0}export function happeningNowEligible(s){return momentEligible(s)&&currentSource(s)&&["LIVE_VIDEO","LIVE_IMAGE","EXTERNAL_LIVE","PARTNER"].includes(s.truth)}export function rankMoments(sources){return(sources||[]).filter(momentEligible).sort((a,b)=>{const ac=currentSource(a)?1:0,bc=currentSource(b)?1:0;if(ac!==bc)return bc-ac;const as=(a.moment||0)*.55+(a.quality||0)*.35+sourceScore(a)*.1,bs=(b.moment||0)*.55+(b.quality||0)*.35+sourceScore(b)*.1;return bs-as})}export function bestMoment(sources,family){const pool=family==="Earth Happening Now"?(sources||[]).filter(happeningNowEligible):sources||[];return rankMoments(pool.filter(s=>(s.categories||[]).includes(family)))[0]||null}
