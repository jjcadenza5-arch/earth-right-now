import { discoverableSource } from "./discovery-eligibility.js";import { recencyState } from "./source-recency.js";const editorial=["Beautiful Earth","Interesting Earth","Useful Earth","Earth Happening Now"];export function momentFamilies(s){return(s.categories||[]).filter(c=>editorial.includes(c))}export function momentEligible(s){return discoverableSource(s)&&momentFamilies(s).length>0}export function happeningNowEligible(s){return momentEligible(s)&&s.health==="HEALTHY"&&s.permission!=="UNKNOWN"&&recencyState(s)==="CURRENT_CHECK"&&["LIVE_VIDEO","LIVE_IMAGE","EXTERNAL_LIVE","PARTNER"].includes(s.truth)}export function rankMoments(sources){return sources.filter(momentEligible).sort((a,b)=>((b.moment||0)*.55+(b.quality||0)*.45)-((a.moment||0)*.55+(a.quality||0)*.45))}export function bestMoment(sources,family){const pool=family==="Earth Happening Now"?sources.filter(happeningNowEligible):sources;return rankMoments(pool.filter(s=>(s.categories||[]).includes(family)))[0]||null}
