const editorial=["Beautiful Earth","Interesting Earth","Useful Earth","Earth Happening Now"];export function momentFamilies(s){return (s.categories||[]).filter(c=>editorial.includes(c))}export function rankMoments(sources){return sources.filter(s=>momentFamilies(s).length).sort((a,b)=>((b.moment||0)*.55+(b.quality||0)*.45)-((a.moment||0)*.55+(a.quality||0)*.45))}export function bestMoment(sources,family){return rankMoments(sources.filter(s=>(s.categories||[]).includes(family)))[0]||null}
