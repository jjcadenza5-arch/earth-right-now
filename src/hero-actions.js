import { playbackCapability } from "./playback-capability.js";import { heroEligible } from "./hero-eligibility.js";export function heroActionState(source,{watchEarthCount=0,now=new Date()}={}){const capability=playbackCapability(source,{now}),enabled=heroEligible(source,{now})&&capability.action!=="UNAVAILABLE";return{watchLive:{enabled,label:enabled?capability.label:"Current window unavailable"},watchEarth:{enabled:watchEarthCount>0,label:"Watch Earth"},myEarth:{enabled:true,label:"My Earth"},explore:{enabled:true,label:"Explore"},next:{enabled,label:"Next live"}}}
