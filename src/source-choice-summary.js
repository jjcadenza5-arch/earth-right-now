import { discoverableSource,currentSource } from "./discovery-eligibility.js";import { playbackCapability } from "./playback-capability.js";
export function sourceChoiceCounts(place,{now=new Date()}={}){
 const sources=(place?.sources||[]).filter(s=>discoverableSource(s)&&playbackCapability(s,{now}).action!=="UNAVAILABLE"),reference=sources.filter(s=>s.truth==="PREVIEW"||s.playback==="PREVIEW").length,liveLike=sources.filter(s=>s.truth!=="PREVIEW"&&s.playback!=="PREVIEW"),inside=liveLike.filter(s=>playbackCapability(s,{now}).action==="PLAY").length,external=liveLike.filter(s=>playbackCapability(s,{now}).action==="EXTERNAL").length,current=liveLike.filter(s=>currentSource(s,{now})).length;
 const unchecked=Math.max(0,liveLike.length-current);return{available:sources.length,inside,external,reference,current,unchecked};
}
export function sourceChoiceSummary(place,options={}){const x=sourceChoiceCounts(place,options),parts=[];if(x.current)parts.push(x.current+" current");if(x.unchecked)parts.push(x.unchecked+" current check unconfirmed");if(x.inside)parts.push(x.inside+" inside ERN");if(x.external)parts.push(x.external+" at source");if(x.reference)parts.push(x.reference+" reference "+(x.reference===1?"image":"images")+" · not live");return parts.join(" · ")||"No available views";}
