import { playbackCapability } from "./playback-capability.js";import { currentWindowAction } from "./current-window-label.js";export function sourceActionMeta(source){const cap=playbackCapability(source),label=currentWindowAction(source);return{label,external:cap.action==="EXTERNAL",disabled:cap.action==="UNAVAILABLE",aria:(cap.action==="EXTERNAL"?label+" for "+source.title+" (opens provider)":label+" for "+source.title)}}
