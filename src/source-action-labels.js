import { playbackCapability } from "./playback-capability.js";import { currentWindowAction } from "./current-window-label.js";
export function sourceActionMeta(source,{now=new Date()}={}){const cap=playbackCapability(source,{now}),label=currentWindowAction(source,{now});return{label,external:cap.action==="EXTERNAL",disabled:cap.action==="UNAVAILABLE",aria:(cap.action==="EXTERNAL"?label+" for "+source.title+" (opens provider)":label+" for "+source.title)}}
