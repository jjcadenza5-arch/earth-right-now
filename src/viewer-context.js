let opener=null;export function rememberOpener(el=document.activeElement){if(opener?.isConnected)return opener;opener=el instanceof HTMLElement&&!el.closest?.("#viewer")?el:null;return opener}export function restoreOpener(){const target=opener;opener=null;if(target?.isConnected)requestAnimationFrame(()=>target.focus({preventScroll:true}))}export function setViewerOpen(open){document.body.dataset.viewerOpen=open?"true":"false";if(open)rememberOpener()}
