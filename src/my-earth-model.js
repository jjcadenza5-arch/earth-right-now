import { loadFavorites } from "./favorites.js";import { loadFavoritePlaces } from "./place-favorites.js";import { loadRecentPlaces } from "./recent-places.js";import { discoverableSource } from "./discovery-eligibility.js";export function buildMyEarth({sources,places}){const sourceFav=loadFavorites(),placeFav=loadFavoritePlaces(),recent=loadRecentPlaces(),placeMap=new Map((places||[]).map(p=>[p.id,p])),sourceMap=new Map((sources||[]).map(s=>[s.id,s]));const favoriteWindows=[...sourceFav].map(id=>sourceMap.get(id)).filter(Boolean),availableFavoriteWindows=favoriteWindows.filter(discoverableSource);return{favoritePlaces:[...placeFav].map(id=>placeMap.get(id)).filter(Boolean),favoriteWindows,availableFavoriteWindows,unavailableFavoriteWindows:favoriteWindows.filter(s=>!discoverableSource(s)),recentPlaces:recent.map(id=>placeMap.get(id)).filter(Boolean)}}export function myEarthCount(model){return model.favoritePlaces.length+model.favoriteWindows.length}
