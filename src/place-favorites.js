const KEY="ern:favorite-places:v1";export function loadFavoritePlaces(){try{return new Set(JSON.parse(localStorage.getItem(KEY)||"[]"))}catch{return new Set()}}export function saveFavoritePlaces(set){localStorage.setItem(KEY,JSON.stringify([...set]));return set}export function toggleFavoritePlace(id){const s=loadFavoritePlaces();s.has(id)?s.delete(id):s.add(id);saveFavoritePlaces(s);return s.has(id)}
