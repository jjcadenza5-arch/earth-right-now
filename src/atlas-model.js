import { atlasFilter } from "./atlas-filters.js";import { clusterSources } from "./atlas-cluster.js";export function buildAtlas(sources,filters={}){const filtered=atlasFilter(sources,filters),mappable=filtered.filter(s=>Number.isFinite(s.lat)&&Number.isFinite(s.lon)),unmapped=filtered.filter(s=>!Number.isFinite(s.lat)||!Number.isFinite(s.lon)),clusters=clusterSources(mappable);return{filtered,mappable,unmapped,clusters,count:filtered.length,mappedCount:mappable.length,unmappedCount:unmapped.length}}export function clusterTitle(cluster){const sources=cluster.sources||[];if(sources.length===1)return sources[0].title;const countries=[...new Set(sources.map(s=>s.country).filter(Boolean))];return`${sources.length} windows${countries.length===1?" · "+countries[0]:""}`}
