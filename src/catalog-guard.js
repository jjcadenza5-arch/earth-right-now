import { validateSource } from "./source-validator.js";function canonicalUrl(value){try{const u=new URL(value);u.hash="";u.hostname=u.hostname.toLowerCase();if((u.protocol==="https:"&&u.port==="443")||(u.protocol==="http:"&&u.port==="80"))u.port="";return u.toString()}catch{return null}}export function guardCatalog(rows){const valid=[],rejected=[],ids=new Set(),sourceUrls=new Map();for(const row of rows){const errors=validateSource(row);const id=typeof row?.id==="string"?row.id.trim().toLowerCase():"";if(id&&ids.has(id))errors.push("duplicate id");if(id)ids.add(id);const sourceUrl=canonicalUrl(row?.sourceUrl);if(sourceUrl&&sourceUrls.has(sourceUrl))errors.push("duplicate sourceUrl with "+sourceUrls.get(sourceUrl));else if(sourceUrl)sourceUrls.set(sourceUrl,row?.id||"unknown");(errors.length?rejected:valid).push(errors.length?{row,errors}:row)}return{valid,rejected}}export function registryFromGuard(rows){const{valid,rejected}=guardCatalog(rows);return{registry:new Map(valid.map(row=>[row.id,Object.freeze(row)])),rejected}}
