const INTENTS=new Set(["stay","eat","transport","tickets"]);export function travelContext(place){if(!place)return null;return{placeId:place.id,title:place.title,region:place.region,country:place.country,coordinates:Number.isFinite(place.lat)&&Number.isFinite(place.lon)?{lat:place.lat,lon:place.lon}:null,intents:["stay","eat","transport","tickets"]}}export function bridgeReady(place){return Boolean(place&&place.country&&(place.region||place.title))}export function travelOffer(input,{place,verified=false}={}){if(!bridgeReady(place)||!input||!INTENTS.has(input.intent))return null;let url=null;try{const u=new URL(input.url);if(!["http:","https:"].includes(u.protocol))return null;url=u.toString()}catch{return null}return{id:String(input.id||"").trim(),placeId:place.id,intent:input.intent,title:String(input.title||"").trim(),provider:String(input.provider||"").trim(),url,affiliate:Boolean(input.affiliate),sponsored:Boolean(input.sponsored),verified:Boolean(verified),verifiedAt:verified?(input.verifiedAt||new Date().toISOString()):null}}export function visibleTravelOffer(offer){return Boolean(offer?.verified&&offer.id&&offer.title&&offer.provider&&offer.url)}export function travelDisclosure(offer){if(!visibleTravelOffer(offer))return null;if(offer.sponsored)return"Sponsored travel option";if(offer.affiliate)return"Affiliate travel option";return"Travel option"}
