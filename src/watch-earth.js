import { sourceStatus } from "./health-policy.js";import { recencyState } from "./source-recency.js";import { playbackCapability } from "./playback-capability.js";export function watchEarthEligible(s){return!!s&&sourceStatus(s).live&&s.health==="HEALTHY"&&s.permission!=="UNKNOWN"&&recencyState(s)==="CURRENT_CHECK"&&playbackCapability(s).action!=="UNAVAILABLE"}export function buildWatchEarth(sources,{limit=20}={}){return sources.filter(watchEarthEligible).sort((a,b)=>((b.moment||0)*.45+(b.quality||0)*.4+(b.freshness||0)*.15+(playbackCapability(b).action==="PLAY"?10:0))-((a.moment||0)*.45+(a.quality||0)*.4+(a.freshness||0)*.15+(playbackCapability(a).action==="PLAY"?10:0))).slice(0,limit)}export function watchEarthFallback(sources,{limit=20}={}){return buildWatchEarth(sources,{limit})}
