import fs from "node:fs";import { watchEarthSnapshot,buildWatchEarth,watchEarthEligible } from "../src/watch-earth.js";
const sources=JSON.parse(fs.readFileSync(new URL("../data/sources.json",import.meta.url),"utf8"));
const checks=sources.flatMap(s=>[s.lastSuccessfulCheck,s.checkedAt]).map(Date.parse).filter(Number.isFinite),anchor=new Date(Math.max(...checks)),day=anchor.toISOString().slice(0,10);
const hours=[0,6,12,18],rows=hours.map(hour=>{const now=new Date(`${day}T${String(hour).padStart(2,"0")}:00:00Z`),items=buildWatchEarth(sources,{limit:20,now}),snap=watchEarthSnapshot(sources,{limit:20,now}),ids=items.map(s=>s.id),uniqueIds=new Set(ids),eligible=items.filter(s=>watchEarthEligible(s,{now})).length,previews=items.filter(s=>s.truth==="PREVIEW").length;return{utc:now.toISOString(),...snap,eligible,duplicateIds:ids.length-uniqueIds.size,previews,titles:items.map(s=>s.title)}});console.log(JSON.stringify({anchorCheck:anchor.toISOString(),audits:rows},null,2));
