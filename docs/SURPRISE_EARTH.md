# Surprise Earth

Surprise Me may select any usable Earth window, including a legitimate external current source, but it must use the same capability-aware action as every other surface.

It never sends an EXTERNAL source into the immersive iframe/player merely because it was selected randomly.
