# Playback accessibility labels

Every embedded frame and refreshed image has a non-empty accessible label even if a malformed/recovered catalog row lacks a title.

Normal source titles remain preferred. Generic ERN labels are fallback-only and do not alter source truth.
