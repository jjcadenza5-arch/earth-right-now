# Earth Signal operator status

The status report is an internal truth surface derived from the production capability manifest. It distinguishes work that is architecturally designed from infrastructure that is actually deployed.

Current default status is **READ_ONLY** with six infrastructure blockers. That is expected and healthy while ERN is building the feature safely.

A future operator/release surface can consume this report, but it must not manually override a blocker or infer readiness from the existence of code, documentation or prototypes.
