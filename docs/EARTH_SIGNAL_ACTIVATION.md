# Earth Signals activation gate

Earth Signals remain read-only architecture until every launch dependency is real and verified.

Required capabilities: submission transport, rate limiting, moderation, reporting, automatic expiry/deletion, and an updated privacy notice. Missing any one capability keeps contribution disabled.

This gate is intentionally separate from UI enthusiasm or business priority. ERN must not expose a working-looking contribution control that cannot safely complete the action.
