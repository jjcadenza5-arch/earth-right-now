# Choose a Window contract

The window-strip controller now owns an actionable-only list.

Offline/unavailable rows cannot remain selectable merely because an older place record still contains them. The first selected window is therefore always one the canonical playback policy can act on.

"Has choice" means more than one actionable window, not more than one historical camera row.
